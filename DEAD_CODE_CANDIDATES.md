# DEAD_CODE_CANDIDATES.md

> لا يُحذف شيء من هذه القائمة قبل إثبات عدم استخدامه وتوثيقه (§47).

| المرشّح | ملاحظة | حالة الإثبات |
|---|---|---|
| `dataconnect/` | سقالة Firebase Data Connect غير مستخدمة (موثّق في `CLAUDE.md`) | لم يُثبت |
| `functions/index.js` | Cloud Functions غير منشورة (خطة Spark) — **لا تُحذف**، تُنشر عند الترقية | يُحتفظ به عمداً |
| `.code-split-metadata.json` · `*.part` | بقايا عملية تقسيم ملفات سابقة | لم يُثبت |
| اعتمادية `firebase` في `package.json` | الإنتاج يستورد 11.0.0 من CDN؛ الحزمة للاختبارات فقط | يُوضَّح لا يُحذف |
| وثائق الجذر التاريخية | `IMPLEMENTATION_SUMMARY.md` · `PROJECT_COMPLETE.md` … تقارير حالة قديمة | لم يُثبت |

**الطريقة:** بحث عن المراجع والاستيرادات والاستخدام في قاعدة البيانات والمسارات والاختبارات → إثبات → توثيق → حذف في إيداع منفصل قابل للتراجع.
