# ACCOUNTING_INTEGRITY_FIX_PLAN.md

> **خطة لا تنفيذ.** لم يُصلَح أي بند هنا في Phase 2 (شرطك 13).
> التنفيذ في Phase 4 (خدمات الأعمال) بعد Phase 3 (المستودعات) وبموافقة صريحة.

---

## 1. مشكلة التحقق على مستوى الترويسة

### الحالي
`database.rules.json` تحرس:
```
newData.child('totalDebit').val() === newData.child('totalCredit').val()
```
تقارن رقمين مخزّنين في الترويسة. لا علاقة لها بالسطور.

### الحدّ التقني
لغة قواعد RTDB **لا تملك تجميعاً على المصفوفات**. لا `sum()` ولا تكرار.
فالتحقق من `Σ lines == header` **مستحيل في القاعدة**، ليس صعباً بل غير ممكن.

### النتيجة
الحراسة تمنع صنفاً واحداً من الخطأ (ترويسة غير متوازنة) وتترك الأخطر:
ترويسة متوازنة فوق سطور ليست كذلك.

---

## 2. مشكلة التحقق على مستوى السطور

### الحالي
حارسٌ واحد في الواجهة — `accounting.js:3679`:
```js
if (Math.abs(dr - cr) >= 0.01) { toast('القيد غير متوازن'); return; }
```

### الثغرة
مسارات إنشاء القيود **سبعة**، والحارس على واحد منها فقط (القيد اليدوي).
الستّة الأخرى (`createJournalForSInv` · `PInv` · `Voucher` · `CreditNote` ·
`DebitNote` · `PMC`) تبني `lines` و`totalDebit`/`totalCredit` برمجياً **بلا
تحقق نهائي مشترك**. صحّتها اليوم مسألة صحّة كل مسار على حدة.

### الحالة المثبتة في الاختبار
`tests/fixtures/journal-entries.json` → الحالة `unbalanced-lines-balanced-header`:
ترويسة 1000 وسطور 5000. **تقبلها القواعد اليوم.**

### الحل المقترح
`assertBalanced(entry)` في النطاق — دالة نقيّة تفحص:
1. `Σ lines.debit == totalDebit` ضمن تسامح
2. `Σ lines.credit == totalCredit`
3. `totalDebit == totalCredit`
4. كل سطر له `accountCode` موجود، ولا سطر بمدين ودائن معاً
5. لا سطر صفري في الطرفين

تُستدعى من **نقطة ترحيل مركزية واحدة** تمرّ بها المسارات السبعة (§5).

**⚠️ حدّ صريح:** هذا يغلق الثغرة أمام التطبيق، **لا أمام كتابة مباشرة على
RTDB بمفتاح صالح**. الإغلاق التام يتطلّب قاعدة علائقية بقيد `CHECK`/`TRIGGER`
أو خادماً وسيطاً — كلاهما خارج نطاق هذا المشروع.

---

## 3. مشكلة الذرّية

### الحالي — تسلسل `postPInv` مقروءاً من الشفرة

| # | الكتابة | الموضع |
|---|---|---|
| 1 | `push(R.jrn, jrnData)` | داخل `createJournalForPInv` |
| 2 | `update(purchaseInvoices/$key, {journalEntryKey, journalEntryNumber})` | داخل `createJournalForPInv` |
| 3 | `update(purchaseInvoices/$key, {status:'posted', postedAt, postedBy})` | `postPInv` |
| 4 | `createInventoryMovementsForPInv()` | `postPInv` |

أربع كتابات مستقلة، لكلٍّ `await` منفصل، **بلا معاملة**.

### القدرة الموجودة وغير المستغلّة
RTDB **تدعم** التحديث الذرّي متعدّد المسارات:
```js
update(ref(db), { 'ledger/a': x, 'ledger/b': y })   // ذرّي: الكل أو لا شيء
```
والنظام يستخدمه في مواضع أخرى، ولديه `scopeUpdates()` جاهز يضيف بادئة المستأجر
للمفاتيح المطلقة.

**أي أن الإصلاح لا يتطلّب تغيير مخطّط ولا انتقالاً لقاعدة أخرى.**

### الحل المقترح
```js
const newJrnKey = push(child(ref(db), 'ledger/journalEntries')).key;
await update(ref(db), scopeUpdates({
  [`ledger/journalEntries/${newJrnKey}`]: entry,
  [`ledger/purchaseInvoices/${key}/status`]: 'posted',
  [`ledger/purchaseInvoices/${key}/journalEntryKey`]: newJrnKey,
  [`ledger/inventoryMovements/${mvKey}`]: movement,
  [`ledger/postingLocks/pinv:${key}`]: { at, by }   // مفتاح منع التكرار
}));
```

**حدّ صريح:** الذرّية على مستوى الكتابة لا على مستوى القراءة-ثم-الكتابة.
قراءة الحالة قبل البناء تبقى خارج المعاملة (§4).

---

## 4. سيناريوهات الفشل الجزئي

| السيناريو | الحالة بعد الفشل | الكشف اليوم | الخطورة |
|---|---|---|---|
| فشل بعد ١ | قيد يتيم في الدفتر، الفاتورة مسوّدة | ❌ لا شيء | 🔴 ميزان المراجعة يتضخّم بقيد لا مصدر له |
| فشل بعد ٢ | الفاتورة تشير لقيد وهي مسوّدة | ❌ | 🟠 |
| فشل بعد ٣ | فاتورة مُرحَّلة بقيد، بلا حركات مخزون | ❌ | 🔴 المخزون ناقص والتكلفة خاطئة |
| فشل في `postVoucher` أثناء تخصيص n فاتورة | سند مُرحَّل، بعض الفواتير مُحدَّثة | ❌ | 🔴 **رصيد عميل/مورد خاطئ** |

**الملاحظة الجوهرية:** كل هذه الحالات **غير مكتشفة**. لا فحص اتّساق دوري،
ولا تسوية بين الدفتر والمستندات.

**تخفيف قائم ومحمود:** ترتيب «القيد أولاً» في `postPInv`/`postSInv` مقصود
وموثّق بتعليق في الشفرة — يغطي أسوأ الحالات (فاتورة مُرحَّلة بلا قيد).

---

## 5. سيناريوهات الترحيل المزدوج

### الحارس الحالي
```js
if (inv.status !== 'draft') return;
```
يقرأ من `window.purchaseInvoices` — **ذاكرة محلية** يملؤها `onValue`.

### نافذة السباق
1. المستخدم ينقر «ترحيل» مرتين خلال جزء من الثانية
2. الاستدعاءان يقرآن `status === 'draft'` (لم يصل تحديث `onValue` بعد)
3. كلاهما يُنشئ قيداً ⇒ **قيدان لنفس الفاتورة**

### مسارات أخرى للتكرار
- إعادة المحاولة بعد انقطاع شبكة (المستخدم لا يعرف هل نجحت)
- تحديث الصفحة أثناء الترحيل
- زرّ الترحيل **لا يُعطَّل** أثناء العملية (بخلاف زرّ حفظ القيد اليدوي الذي يُعطَّل)

### الحل المقترح
1. **مفتاح Idempotency:** `ledger/postingLocks/{sourceType}:{sourceKey}` يُكتب
   داخل نفس التحديث الذرّي. وجوده يمنع الترحيل الثاني.
2. **قفل تفاؤلي:** التحقق من `status === 'draft'` **داخل** `runTransaction`
   لا من الذاكرة.
3. تعطيل الزرّ أثناء العملية (تحسين تجربة، لا ضمان).

**الطبقة 1 هي الضمان الحقيقي** — 2 و3 دفاع في العمق.

---

## 6. الاختبارات المطلوبة قبل أي إصلاح

| # | الاختبار | يسبق |
|---|---|---|
| 1 | Golden Master لكل مسار من السبعة: مدخلات ثابتة ⇒ سطور القيد المتوقّعة | كل شيء |
| 2 | `assertBalanced` على الحالات الأربع في `tests/fixtures/journal-entries.json` | §2 |
| 3 | ترحيل فاتورة مشتريات: القيد + الحالة + حركات المخزون على محاكي | §3 |
| 4 | حقن فشل بعد كل كتابة والتحقق من الحالة الناتجة | §4 |
| 5 | ترحيل متزامن مرّتين ⇒ قيد واحد فقط | §5 |
| 6 | تخصيص سند على فاتورتين: الأرصدة بعده | §4 |
| 7 | ميزان المراجعة قبل/بعد كل إصلاح — **يجب ألّا يتغيّر** | كل شيء |

**الاختبار 7 هو بوابة القبول:** أي إصلاح يغيّر ميزان المراجعة على نفس
البيانات هو تغيير سلوك محاسبي، ويخضع لـ§6 كاملاً.

---

## 7. الترتيب المقترح للتنفيذ

```
Phase 3 — المستودعات        ← يوفّر مكان الكتابة الذرّية
   ↓
Phase 5 — Golden Master     ← شبكة الأمان (الاختبارات 1 و7)
   ↓
Phase 4 — خدمات الأعمال     ← نقطة ترحيل مركزية + assertBalanced + Idempotency
```

**ملاحظة على الترتيب:** أقترح تقديم Phase 5 على Phase 4 خلافاً للترقيم،
لأن الإصلاح بلا شبكة أمان يخالف مبدأك في §14. القرار لك.

---

## 8. مُكتشَفات Phase 4 — مُثبتة بالتشغيل لا بالقراءة

> أُضيفت بعد التقاط السلوك الفعلي من `accounting.js`. **لم يُصلَح أيٌّ منها.**

### 8.1 انعدام Idempotency — مُثبت

**السلوك الحالي:** استدعاء `createJournalForPInv` مرّتين بنفس `sourceKey` يُنتج
**قيدين كاملين** بنفس المبالغ ونفس المصدر. لا فحص ولا مفتاح يمنع ذلك.

**لماذا قد يكون خطأً:** نقر مزدوج أو إعادة محاولة بعد انقطاع شبكة تُضاعف
الالتزام في الدفتر.

**الأثر المالي:** مضاعفة رصيد المورد ⇒ احتمال **سداد مرّتين**. في فاتورة
11,500 ريال يعني 11,500 ريال خسارة نقدية مباشرة لكل حادثة.

**السلوك المستهدف:** الاستدعاء الثاني بنفس `sourceType:sourceKey` لا يُنشئ شيئاً
ويعيد مفتاح القيد القائم.

**الإصلاح المقترح:** `ledger/postingLocks/{sourceType}:{sourceKey}` يُكتب داخل
نفس التحديث الذرّي؛ وجوده يمنع الثاني.

**الاختبارات المطلوبة:** استدعاء متزامن مرّتين ⇒ قيد واحد · إعادة محاولة بعد
فشل ⇒ قيد واحد · القفل يُكتب ذرّياً مع القيد لا قبله ولا بعده.

---

### 8.2 القيد اليتيم — مُثبت بحقن فشل

**السلوك الحالي:** حُقن فشل شبكة عند الكتابة الثانية. النتيجة المرصودة:
القيد **دُفع فعلاً**، والفاتورة **لم تُربط به**، والخطأ صعد للمستدعي.

**الأثر المالي:** قيدٌ في الدفتر لا تعرفه أي فاتورة ⇒ ميزان المراجعة يتضخّم
بمبلغ لا مصدر له. ولا فحص اتّساق دوري يكشفه ⇒ يبقى حتى الإقفال.

**السلوك المستهدف:** إمّا يُكتب كل شيء أو لا شيء.

**الإصلاح المقترح:** تحديث ذرّي متعدّد المسارات (§3).

**الاختبارات المطلوبة:** حقن فشل بعد كل نقطة كتابة ⇒ الحالة النهائية نظيفة في
كل الحالات · فحص اتّساق يكشف القيود اليتيمة القائمة.

---

### 8.3 `ensureStdAccount` تُنشئ حسابات بلا سؤال — مرصود

**السلوك الحالي:** عند غياب حساب قياسي (مثل 2140 ضريبة المخرجات)، تُنشئه
تلقائياً في شجرة حسابات المستخدم أثناء الترحيل، مع `toast` إشعاري.

**لماذا قد يكون خطأً:** يعدّل شجرة الحسابات — وهي بيانات رئيسية — كأثر جانبي
لعملية ترحيل، بلا موافقة صريحة ولا مسار اعتماد.

**الأثر المالي:** منخفض مباشرةً (الحساب من قائمة قياسية)، لكنه يعني أن شجرة
الحسابات قد تنمو بحسابات لم يقرّرها المحاسب.

**السلوك المستهدف:** قرارك — إمّا إبقاؤه (يمنع فشل الترحيل) أو تحويله إلى
اقتراح يتطلّب تأكيداً.

**⚠️ ملاحظة:** هذا **ليس عيباً مؤكّداً** بل قرار تصميم يستحق مراجعتك.

---

### 8.4 ما ثبت أنه سليم — لا يحتاج إصلاحاً

| البند | الدليل |
|---|---|
| الأرصدة محسوبة لا مخزّنة | يمنع الانحراف بين الرصيد والحركات · يُلغي سيناريو «فشل تحديث الرصيد» |
| تاريخ القيد من المستند | BUG-001 لا يمسّ تاريخ القيد |
| فشل بناء القيد ⇒ لا ترحيل | يمنع أسوأ الحالات: فاتورة مُرحَّلة بلا قيد |
| القيود المُولَّدة متوازنة | 74 تأكيداً على مبالغ من 0 إلى 999,999.99 |
| ضريبة صفرية ⇒ لا سطر ضريبة | سليم محاسبياً |

---

## 9. مُكتشَفات Phase 5 — أثر §3/§4/§5 على التقارير المحاسبية فعلياً

> Phase 4 (§8 أعلاه) أثبتت **أن** الترحيل غير ذرّي وغير Idempotent، على مستوى
> عدّاد الكتابات. Phase 5 التقطت **أين يظهر** هذا فعلياً في التقارير التي
> يفتحها المستخدم — والنتيجة مقلقة أكثر ممّا افترضناه في §8: الأثر **متباين**
> بين الشاشات لا موحّداً.

### 9.1 قيد مكرَّر: يظهر في ميزان المراجعة، لا يظهر في رصيد الطرف — BUG-007

`tbCalcBalances`/`coaAccountOps` تُبنَيان من **القيود**؛ `calcCustomerBalance`/
`calcVendorBalance` تُبنَيان من **الفواتير**. قيد مكرَّر (من §5) يُضاعف رقماً
في الأول ولا يُلمَس في الثاني — **تناقض صامت بين شاشتين لكل نفس الحدث
المحاسبي**، بلا أي تنبيه يربط بينهما. مُثبَت في
`tests/golden-master/idempotency.test.mjs`. تفصيل كامل: `BUGS_TO_FIX.md BUG-007`.

**أثر على أولوية الإصلاح:** هذا يرفع من إلحاح §5 (مفتاح Idempotency) —
الخطر ليس فقط «قيد إضافي» بل «تقرير رسمي (ميزان المراجعة) يحمل رقماً
تدقيقياً خاطئاً تماماً بينما الشاشة التشغيلية المعتادة (رصيد المورد) تبدو
سليمة». هذا بالضبط النمط الذي يُكتشَف متأخّراً جداً — عند التدقيق السنوي لا
عند الإدخال اليومي.

### 9.2 القيد اليتيم (§4): نفس النمط بالضبط — لا يحتاج بند إضافي

`tests/golden-master/failure-injection.test.mjs` أعاد إنتاج سيناريو §4
«فشل بعد ١» وتتبّع أثره حتى `tbCalcBalances` مباشرةً: يدخل بكامل قيمته،
والميزان يبقى متوازناً (لا إشارة حمراء)، ولا فاتورة تشير إليه للتتبّع
العكسي. يؤكّد استنتاج §4 بدليل تنفيذي إضافي — لا يغيّر التشخيص.

### 9.3 `ensureStdAccount` (§8.3): التكرار مُثبَت الآن، لا مُرصوداً فقط

§8.3 وصفت الإنشاء التلقائي كـ«قرار تصميم يستحق مراجعة». Phase 5 أضافت
حقيقة تقنية إضافية: **الإنشاء نفسه غير Idempotent** تحت تزامن حقيقي (نافذة
بين `push()` واكتمال `onValue`) — تفصيل كامل: `BUGS_TO_FIX.md BUG-006`،
`docs/accounting/ensureStdAccount.md`. هذا يبقيها قراراً للمالك كما كانت،
لكنه يوسّع نطاق القرار: ليس فقط «هل يُنشئ حسابات بلا سؤال؟» بل أيضاً «هل
قد يُنشئ **نسختين** من نفس الحساب القياسي؟»

### 9.4 اكتشاف جديد مستقلّ — BUG-005: `tbCalcBalances` ≠ `calcFSBalances`

خارج نطاق §1–§8 تماماً: محرّكا الأرصدة الرئيسيان في النظام (ميزان المراجعة
المخصَّص مقابل محرّك القوائم المالية المشترك) يختلفان في معاملة الحركات
السابقة للفترة غير المُعلَّمة افتتاحياً. تفصيل كامل: `BUGS_TO_FIX.md BUG-005`،
`docs/accounting/trial-balance.md`. **لا علاقة له بالذرّية أو التكرار** —
عطل مستقلّ في منطق الحساب نفسه، اكتُشف فقط لأن Phase 5 التقطت الدالتين
معاً على نفس البيانات ولاحظت التباين.
