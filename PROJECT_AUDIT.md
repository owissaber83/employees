# PROJECT_AUDIT.md — فحص Phase 0

> **الحالة:** فحص فقط. لم يُعدَّل أو يُحذف أي ملف، ولم تُغيَّر قاعدة بيانات ولا قواعد أمان ولا منطق محاسبي.
> **التاريخ:** 2026-08-19 · **الفرع:** `feat/ai-invoice-system-v2` · **آخر إيداع:** `71f2b36`

---

## 0. تصحيحان قبل البدء

هذان التصحيحان يغيّران استنتاجات في الطلب الأصلي، فأضعهما أولاً:

**1) النظام لا يستخدم Firestore — بل Realtime Database.**
ورد في الطلب «افحص Firestore». لا يوجد أي استخدام لـFirestore في الشفرة. النظام يستخدم Firebase **Realtime Database** (شجرة JSON عبر `getDatabase`/`ref`/`onValue`). الفرق جوهري للترحيل: RTDB بلا استعلامات مركّبة ولا تجميع خادمي ولا معاملات متعدّدة المسارات بالمعنى العلائقي، وهذا يفسّر معظم الدين التقني الموصوف أدناه.

**2) `CLAUDE.md` متأخّر عن الواقع بفارق كبير.**

| | ما يقوله التوثيق | الواقع المقيس |
|---|---|---|
| `app.js` | ~23,000 سطر | **31,430** |
| `index.html` | ~6,700 سطر | **20,353** |
| عدد الصفحات | ~51 | **142** |

التوثيق نفسه أصبح مصدر مخاطرة: من يعتمد عليه في تقدير الجهد سيخطئ بنسبة تتجاوز 100%.

---

## 1. المعمارية الحالية

**تطبيق صفحة واحدة (SPA) مكتوب يدوياً، بلا إطار عمل وبلا خطوة بناء.**

```
public/index.html   ← كل الترميز: 142 قسماً <div class="pg" id="pg-*">
      │
      ├── app.js          (ES Module)  ← النواة: Firebase · الحالة · التنقّل · الصلاحيات · HR · المشاريع · المشتريات
      ├── accounting.js   (defer عادي) ← المحاسبة الكاملة: القيود · الدفاتر · القوائم · الضرائب · المخزون
      ├── project-detail.js (defer)
      ├── analytics.js     (defer)
      ├── help.js          (defer)
      ├── pdfeditor-*.js  (4 ملفات · window.PDFE/PDE)   ← وحدة مُطبَّقة الطبقات
      └── aiinvoice-*.js  (3 ملفات · window.AINV/AIU)   ← وحدة مُطبَّقة الطبقات
```

التنقّل ليس توجيهاً: `nav(pg)` يبدّل صنف `act` على الأقسام. لا تحميل كسول، ولا مسارات، ولا عناوين URL لكل صفحة (عدا `#portal=` و`#` محدود).

الملفات الثانوية **ليست وحدات** — تعتمد كلياً على متغيّرات عامة يعلّقها `app.js` على `window`.

---

## 2. المكدّس التقني

| الطبقة | التقنية |
|---|---|
| الواجهة | JavaScript خام · لا إطار · لا bundler · لا خطوة بناء |
| الحالة | متغيّرات عامة على `window` + `onValue` |
| قاعدة البيانات | Firebase Realtime Database |
| المصادقة | Firebase Auth (بريد/كلمة مرور) |
| الاستضافة | Firebase Hosting (يخدم `public/` كما هي) |
| الخطة | **Spark (مجانية)** — لا Cloud Functions منشورة، لا Firebase Storage |
| تخزين الملفات | Cloudinary (رفع غير موقّع) |
| الذكاء الاصطناعي | Gemini مباشر + وسيط Cloudflare Worker |
| الاختبار | Node خام + `@firebase/rules-unit-testing` + puppeteer-core |
| الجودة | ESLint |

**⚠️ تعارض إصدارات:** `package.json` يعلن `firebase: ^12.13.0`، بينما الشفرة تستورد فعلياً **11.0.0** من `gstatic.com`. حزمة npm لا تُشغَّل إطلاقاً في الإنتاج — تُستخدم للاختبارات فقط. أي ترقية لـnpm لن تغيّر شيئاً في الإنتاج، والعكس صحيح.

**مكتبات CDN (8):** chart.js · xlsx · jspdf + autotable · html2canvas · ArabicReshaper · tesseract · pdf.js

---

## 3. القياسات — حجم المشكلة

| المقياس | القيمة | الأثر على الترحيل |
|---|---|---|
| أسطر الشفرة المخدومة | **137,941** | — |
| الحجم الكلي | **9.3 ميجابايت** | يُحمَّل كاملاً في كل زيارة (لا تقسيم) |
| صفحات | **142** | كل صفحة تحتاج إعادة كتابة |
| `onclick=` مضمّنة | **2,522** (706 HTML + 1,816 مولّدة) | 🔴 **العائق الأكبر** |
| `innerHTML` | **975** | 🔴 الواجهة تُبنى كنصوص HTML |
| متغيّرات عامة على `window` | **2,356** | 🔴 لا حدود بين الوحدات |
| مجموعات RTDB في `R` | **130** | |
| مستمعات `onValue` | **97** | كلها تحمّل مجموعات كاملة للذاكرة |
| `try/catch` | 847 | منها 19 تبتلع الخطأ في `console` فقط |

**الاستنتاج الأهم:** الواجهة **غير قابلة للتحويل** ملفاً بملف. `onclick="fn()"` داخل نصّ `innerHTML` مربوط بدالة عامة ليس له مقابل في React — كل صفحة تُعاد كتابتها. من يقدّر هذه المهمة على أنها «تحويل JS إلى JSX» سيخطئ التقدير بمرتبة كاملة.

---

## 4. الوحدات الرئيسية

| المجال | الملف | الحجم |
|---|---|---|
| المحاسبة (قيود · دفاتر · قوائم · ضرائب · مخزون · أصول) | `accounting.js` | 33,532 سطر |
| النواة + HR + الرواتب + المشاريع + المشتريات + الحضور | `app.js` | 31,430 سطر |
| ملف المشروع | `project-detail.js` | 7,221 سطر |
| التحليلات | `analytics.js` | 2,346 سطر |
| محرر PDF | `pdfeditor-*.js` | ~6,000 سطر |
| قراءة الفواتير بالذكاء الاصطناعي | `aiinvoice-*.js` | ~5,600 سطر |

---

## 5. بنية قاعدة البيانات

**متعدّدة المستأجرين، والعزل مُطبَّق فعلاً** — وهذه نقطة قوة معمارية موجودة مسبقاً:

```
tenants/$tid/ledger/{130 مجموعة}
```

العزل يتم بغلاف حول `ref`:

```js
function ref(database, path) {
    if (currentTenantId && path.startsWith('ledger'))
        path = 'tenants/' + currentTenantId + '/' + path;
    return _rawRef(database, path);
}
```

وغلاف ثانٍ `scopeUpdates()` للتحديثات متعدّدة المسارات. **هذا تصميم سليم**: نقطة واحدة تحكم النطاق، ولا تُنسخ في 130 موضعاً.

نمط البيانات: `onValue` يملأ متغيّراً عاماً → دوال العرض تقرأ من الذاكرة. لا ترقيم صفحات، ولا استعلامات، ولا تجميع خادمي.

---

## 6. المصادقة

Firebase Auth عبر `onAuthStateChanged`، ثم بناء `R` بعد معرفة المستأجر. المنطق مبعثر داخل `app.js` ومختلط بالواجهة، لكن نقطة الدخول واحدة — قابلة للاستخلاص إلى `AuthService` بجهد معقول.

---

## 7. الصلاحيات

**نقطة قوة حقيقية.** مصفوفة **119 صلاحية** مُنفَّذة على مستويين:

1. **الواجهة:** `can(p)` يخفي الأزرار والصفحات
2. **قاعدة البيانات:** `permsMap` داخل قواعد الأمان — الإنفاذ الحقيقي

قواعد الأمان تحرس: الدور · `permsMap` · حالة الاشتراك (`accessUntil`) · علم `active` · عزل المستأجر. ومغطّاة بـ**186 تأكيداً** في `npm run test:rules`.

هذا يعني أن الصلاحيات **لا تعتمد على الواجهة وحدها** — شرط مطلوب في الطلب ومُحقَّق مسبقاً.

---

## 8. المنطق المحاسبي — وأخطر ثغرة في النظام

### 8.1 ما هو موجود ونظيف

`public/calc.js` — **وحدة نقيّة بلا DOM ولا Firebase**، مع اختبارات (`test:calc`، 27 تأكيداً). تغطي: المستخلصات، الضريبة، شهادات مقاولي الباطن، هامش الربح. **هذا هو النموذج المطلوب بالضبط، وهو مُثبت في المستودع.** لكنه **65 سطراً فقط** — جزء ضئيل من المنطق المحاسبي.

### 8.2 🔴 الخطر الأول — توازن القيد غير مضمون

قواعد الأمان تحرس التوازن:
```
newData.child('totalDebit').val() === newData.child('totalCredit').val()
```
لكن هذا يقارن **رقمين في الترويسة فقط**. لا يتحقّق أن **مجموع السطور** يساوي الترويسة — و**RTDB عاجزة بنيوياً** عن ذلك (لغة القواعد لا تجمع مصفوفة).

الحارس الحقيقي الوحيد سطرٌ في الواجهة (`accounting.js:3679`):
```js
if (Math.abs(dr - cr) >= 0.01) { toast('القيد غير متوازن'); return; }
```

**النتيجة:** قيدٌ ترويسته متوازنة وسطوره ليست كذلك سيُقبل. خطأ برمجي أو استيراد أو كتابة مباشرة يكسر الدفتر بلا اعتراض.

### 8.3 🔴 الخطر الثاني — لا معاملات ذرّية

`postPInv` ينشئ القيد أولاً ثم يحدّث حالة الفاتورة، مع تعليق صريح في الشفرة يشرح أن الترتيب مقصود كي لا تبقى فاتورة `posted` بلا قيد. **هذا نمط تعويضي يدوي** يغطي غياب المعاملات. إن فشلت الخطوة الثانية، يوجد قيد بلا فاتورة مرحّلة.

### 8.4 حدود أخرى موثّقة

- لا مفاتيح أجنبية ⇒ حذف مشروع يترك يتامى في 16 مجموعة (موثّق في مراجعة سابقة)
- إقفال الفترات مُنفَّذ في JS لا في القاعدة
- ميزان المراجعة والقوائم المالية تُحسب بجلب كل القيود إلى ذاكرة المتصفح

---

## 9. الدين التقني

| # | البند | الخطورة |
|---|---|---|
| 1 | 2,522 معالج `onclick` مضمّن مربوط بدوال عامة | 🔴 |
| 2 | 2,356 متغيّراً عاماً — لا حدود بين الوحدات | 🔴 |
| 3 | 975 `innerHTML` — الواجهة نصوص HTML (سطح XSS + صيانة) | 🔴 |
| 4 | ملفان بـ65,000 سطر مجتمعَين | 🔴 |
| 5 | توازن القيد غير مضمون في القاعدة | 🔴 |
| 6 | 97 مستمعاً يحمّلون مجموعات كاملة للذاكرة | 🟠 |
| 7 | لا تقسيم شفرة — 9.3م.ب في كل زيارة | 🟠 |
| 8 | تعارض إصدار Firebase (12 معلن / 11 مشغَّل) | 🟠 |
| 9 | `CLAUDE.md` متأخّر عن الواقع | 🟠 |
| 10 | 19 موضعاً يبتلع الخطأ في `console` | 🟡 |
| 11 | لا فصل بيئات (dev/staging/prod) — إعدادات مضمّنة | 🟠 |
| 12 | لا نسخ احتياطي خادمي (`dailyBackup` غير منشور على Spark) | 🔴 |

---

## 10. مخاطر الأمان

| البند | الحالة |
|---|---|
| عزل المستأجرين | ✅ مُنفَّذ ومُختبَر (186 تأكيداً) |
| الصلاحيات في القاعدة لا الواجهة | ✅ عبر `permsMap` |
| رؤوس أمان + CSP | ✅ مُفعَّلة (CSP بوضع تقرير) |
| 121 منفذ XSS مُهرَّب | ✅ من تدقيق سابق |
| **975 `innerHTML`** | 🟠 سطح دائم يتطلّب انضباطاً في كل إضافة |
| مفتاح Gemini في المتصفح | 🟠 مقايضة معلنة — يجب تقييده بالنطاق |
| رفع Cloudinary غير موقّع | 🟠 |
| App Check · MFA | 🔴 غير مفعّلين |

---

## 11. مخاطر الأداء

- **9.3 م.ب** بلا تقسيم ولا تحميل كسول
- 97 مستمعاً دائماً — كل تغيير في أي مجموعة يعيد الرسم
- التقارير المالية O(كل القيود) في المتصفح
- لا ترقيم صفحات في أي جدول

**السقف العملي:** يعمل جيداً عند عشرات الآلاف من السجلات، ويختنق عند مئات الآلاف.

---

## 12. مخاطر الترحيل

| # | الخطر | الأثر |
|---|---|---|
| 1 | **React يتطلّب خطوة بناء** — تناقض مباشر مع قاعدة معمارية قائمة في `CLAUDE.md` | 🔴 يجب قرار صريح |
| 2 | 2,522 معالجاً مضمّناً لا مقابل له في React ⇒ إعادة كتابة كل صفحة | 🔴 |
| 3 | 2,356 متغيّراً عاماً — أي جزيرة React تحتاج جسراً هشّاً للحالة | 🔴 |
| 4 | لا اختبارات على الغالبية العظمى من المنطق المحاسبي ⇒ لا شبكة أمان للانحدار | 🔴 |
| 5 | مشروعان متوازيان مطروحان (React + PostgreSQL/استضافة سعودية) | 🔴 تنفيذهما معاً يقتل المشروع |
| 6 | 10 إيداعات غير مدفوعة على جهاز واحد | 🔴 خطر فقد فعلي |
| 7 | مطوّر واحد · نظام إنتاج حيّ يمسّ المال | 🟠 |

---

## 13. الاعتماديات

**اعتمادية تشغيل واحدة:** `firebase` (وهي غير مستخدمة فعلياً في الإنتاج — انظر §2).
**تطوير:** eslint · globals · @firebase/rules-unit-testing.
**النتيجة:** لا شجرة اعتماديات متضخّمة، ولا مخاطر سلسلة توريد تُذكر. هذه ميزة نادرة يجب عدم إهدارها بإضافة عشوائية للمكتبات.

---

## 14. الأصول القائمة — ما يجب البناء عليه لا هدمه

هذه ليست مشروعاً من الصفر. توجد أصول حقيقية:

1. **ثلاث وحدات نقيّة مُثبتة**: `calc.js` · `aiinvoice-engine.js` (2,621 سطر، بلا DOM، 165 تأكيداً) · `pdfeditor-engine.js` (2,220 سطر، بلا DOM، 102 تأكيد). **النمط المعماري المطلوب مُطبَّق ومُختبَر داخل هذا المستودع بالفعل.**
2. **عزل مستأجرين يعمل** بنقطة تحكّم واحدة.
3. **119 صلاحية** مُنفَّذة في القاعدة، بـ186 تأكيداً.
4. **7 مجموعات اختبار** تعمل: rules · calc · pdf · ai · ai:page · ai:e2e · proxy.
5. **ESLint** مضبوط ونظيف.
6. **RTL عربي كامل** ومصطلحات محاسبية سعودية ناضجة (زكاة · قيمة مضافة · استقطاع · ZATCA).

---

## 15. ما يجب **عدم** تغييره

1. ❌ قواعد أمان RTDB وقيمها الحرفية (`'approved'`, `'posted'`, `permsMap`) — تغييرها يُبطل الحراسة بصمت
2. ❌ أسماء حقول قاعدة البيانات أو بنية المجموعات
3. ❌ منطق حساب القيود والأرصدة والمستخلصات قبل تغطيته باختبارات Golden Master
4. ❌ غلافا `ref`/`scopeUpdates` — نقطة عزل المستأجرين
5. ❌ مصفوفة الصلاحيات الـ119
6. ❌ `calc.js` — مصدر الحقيقة للمعادلات المالية
7. ❌ حذف أي ملف قبل إثبات عدم استخدامه (يُسجَّل في `DEAD_CODE_CANDIDATES.md`)
8. ❌ سلوك أي تقرير مالي دون مقارنة رقمية قبل/بعد

---

## 16. ما يجب تغييره

مرتّباً بنسبة العائد إلى المخاطرة:

| الأولوية | التغيير | العائد | المخاطرة |
|---|---|---|---|
| 1 | استخلاص طبقة النطاق المحاسبي (نقيّة، مختبَرة) | عالٍ جداً | **صفر** — لا تمسّ الواجهة |
| 2 | اختبارات Golden Master للعمليات المالية | عالٍ جداً | صفر |
| 3 | طبقة Repository تعزل RTDB | عالٍ | منخفضة |
| 4 | فصل البيئات + إخراج الإعدادات | متوسط | منخفضة |
| 5 | تحديث `CLAUDE.md` ليطابق الواقع | متوسط | صفر |
| 6 | واجهة React تدريجياً | عالٍ | **عالية** |
| 7 | تقسيم الشفرة والتحميل الكسول | متوسط | متوسطة |

**الملاحظة الجوهرية:** البنود 1–3 هي **الشرط المشترك** لمشروعَي React و PostgreSQL معاً. إنجازها أولاً يخدم أي مسار تختاره لاحقاً، ولا يضيع منها شيء مهما تغيّر القرار.
