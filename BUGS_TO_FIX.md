# BUGS_TO_FIX.md

> عيوب **موثّقة ولم تُصلَح عمداً**. كل بند يُعالَج في مرحلة مستقلة بموافقة صريحة،
> وفق §6: توثيق → اختبار → إثبات → تحديد السلوك الحالي والمستهدف → موافقة.
> **لا شيء هنا أُصلح في Phase 2.**

---

## BUG-001 — إزاحة التاريخ يوماً في توقيت الرياض 🟠

**الحالة:** موثّق · غير مُصلَح · محروس باختبار يُثبت السلوك الحالي

### الوصف
```js
const today = new Date().toISOString().slice(0, 10);   // ← UTC
```
`toISOString()` يعطي UTC. في الرياض (UTC+3) تكون النتيجة **يوم أمس** بين
منتصف الليل والثالثة فجراً.

### الانتشار المقيس
| الملف | المواضع |
|---|---:|
| `public/accounting.js` | 110 |
| `public/project-detail.js` | 46 |
| `public/app.js` | 26 |
| **الإجمالي** | **188 تكراراً في 182 سطراً** |

### الأثر المالي
- **أعمار الديون:** فاتورة تُصنَّف «متأخّرة» قبل استحقاقها بيوم (`calcCustomerBalance`)
- **حدود الفترات:** حركة قد تقع في فترة ضريبية خاطئة
- **تواريخ الاستحقاق** المحسوبة بالإضافة على تاريخ خاطئ

### السلوك الحالي (مُثبت باختبار)
`tests/characterization/date-behavior.test.mjs` يثبّت السلوك القائم:
عند `2026-03-10T01:00:00+03:00` يعيد النظام `2026-03-09`.

### السلوك المستهدف
تاريخ محلي: `2026-03-10`. الدالة البديلة موجودة فعلاً في `AINV.todayLocal()`.

### لماذا لم يُصلَح هنا
تصحيحه **يغيّر السلوك** — تواريخ ستتغيّر وأرصدة أعمار ديون ستتغيّر معها.
يستلزم مرحلة مستقلة بمقارنة قبل/بعد على بيانات حقيقية.

### الإصلاح المقترح
1. دالة `todayLocal()` واحدة في النطاق
2. استبدال المواضع الـ188 **دفعةً واحدة** (الجزئي يخلق تعارضاً بين وحدتين)
3. مقارنة أعمار الديون قبل/بعد وتفسير كل فرق

---

## BUG-002 — حجز رمز الحساب لا يُحرَّر عند الحذف 🟠

**الحالة:** موثّق · غير مُصلَح

### الوصف
`saveCoaAccount` (accounting.js:894) يحجز الرمز ذرّياً على الخادم:
```js
const claimRef = ref(db, `ledger/counters/coaCode/${code}`);
await runTransaction(claimRef, current => current ? undefined : true);
```
و`deleteCoaAccount` يحذف الحساب **دون تحرير الحجز**. بحث في الملف كلّه:
`counters/coaCode` يظهر في **موضع واحد فقط** — لا تحرير في أي مكان.

### الأثر
حذف حساب يحرق رمزه إلى الأبد. إعادة إنشائه بنفس الرمز تفشل برسالة مضلّلة:
> «رمز الحساب حُجز للتو من مستخدم آخر — أعد المحاولة برمز مختلف»

والرسالة تشير إلى مستخدم آخر بينما السبب حذفٌ سابق من المستخدم نفسه.

### لماذا لم يُصلَح هنا
يمسّ `deleteCoaAccount` وهي دالة كتابة. الإصلاح تغيير سلوك يستلزم §6.

### الإصلاح المقترح
تحرير الحجز داخل نفس التحديث الذرّي للحذف — يعالَج في Phase 4 مع الذرّية.

---

## BUG-003 — `saveInvItem` مُعرَّفة مرتين بسلوكين مختلفين 🟠

**الحالة:** موثّق · غير مُصلَح

`accounting.js:20758` (55 سطراً) و`:22549` (81 سطراً) · 114 سطر فرق.
في JS يطغى الثاني ⇒ **الأولى شفرة ميتة لا تُنفَّذ**.

**الأثر:** خطر تحرير — من يعدّل السطر 20758 يعدّل ما لا يعمل، ويظنّ أنه أصلح.

**الإصلاح:** حذف الأولى بعد إثبات، في إيداع مستقل. مُسجَّل في `DEAD_CODE_CANDIDATES.md`.

---

## BUG-004 — `createJournalForPMC` مُعرَّفة مرتين 🟡

`accounting.js:1025` و`:2922` — قُورنتا آلياً: **متطابقتان حرفياً** (55 سطراً).
تكرار غير ضارّ سلوكياً، لكنه يضاعف كلفة أي تعديل مستقبلي.
**الإصلاح:** حذف الأولى بعد إثبات.

### **[Phase 7 Step E] — أُعيد التحقّق آلياً · لم يُحذَف أي تعريف**
قُورن التعريفان بايتاً ببايت في هذه المرحلة: **2320 حرفاً · 55 سطراً · متطابقان تماماً**
(`tests/golden-master/project-cost.test.mjs §[0]`، ويُعاد الفحص في كل تشغيل فينكسر
فوراً لو تباعدا). في JS تُرفَع تعريفات الدوال ويفوز **الأخير** (`:2922`) عند التنفيذ،
والتطابق يجعل الفارق منعدماً سلوكياً — ولذلك `extractFunction` (تُرجع الأولى) آمنة
للتوصيف، وهو ما بُنيت عليه خدمة PMC الموازية.

**لم يُحذَف أي تعريف:** الحذف يستلزم تعديل `public/accounting.js` وهو ممنوع بلا تفويض
صريح. الخطر الحقيقي المتبقّي هو **التباعد المستقبلي** لو عُدِّل أحدهما دون الآخر —
وهذا ما يحرسه الاختبار أعلاه الآن.

---

## BUG-005 — `tbCalcBalances` تستبعد بصمت حركات سابقة للفترة غير مُعلَّمة افتتاحياً 🔴

**الحالة:** موثّق (Phase 5) · **غير مُصلَح، ولم يُلمَس في Phase 6** — لا علاقة له بالترحيل الذرّي (منطق تقارير قراءة، لا كتابة). محروس بـ`tests/golden-master/trial-balance.test.mjs §[Y]`

### الوصف
`tbCalcBalances` (accounting.js:11754) وقيد بتاريخ `< tbState.fromDate` **وغير**
مُعلَّم `sourceType:'opening'`:
```js
if (!isOpen && tbState.fromDate && (e.date || '') < tbState.fromDate) return;   // ← يُستبعَد كلياً
```
بينما `calcFSBalances` (accounting.js:23169) لنفس الشرط تماماً:
```js
const asOpening = fsIsOpeningEntry(e) || (!!fromDate && d < fromDate);   // ← يُطوى في «قبل الفترة»
```

### الأثر المالي
ميزان مراجعة لفترة تبدأ منتصف السنة (`fromDate` مضبوط) **يستبعد** حركات
سابقة حقيقية غير مُعلَّمة افتتاحياً من كل من الافتتاحي والحركة — تختفي
بصمت. في المقابل قائمة الدخل/المركز المالي (المبنيّان على `calcFSBalances`)
تطويانها في الرصيد الافتتاحي. **نفس البيانات، رقمان مختلفان في تقريرين
لنفس الشركة لنفس الفترة**، بلا أي تنبيه.

### السلوك الحالي (مُثبت باختبار)
`tests/golden-master/trial-balance.test.mjs §[Y]` — قيد `2025-06-01` غير
مُعلَّم، فترة تبدأ `2026-01-01`: `calcFSBalances.beforeDebit = 700` بينما
`tbCalcBalances` لا يُنتج للحساب صفاً يحمل هذا المبلغ إطلاقاً.

### لماذا لم يُصلَح هنا
يمسّ منطق ميزان المراجعة المُعتمَد فعلياً في الإنتاج. يحتاج قراراً: هل
السلوك الصحيح طيّ الحركة في الافتتاحي (كـ`calcFSBalances`) أم استبعادها
عمداً (كـ`tbCalcBalances`)؟ قرار محاسبي لا تقني، يخضع لـ§6 كاملاً.

### الإصلاح المقترح
توحيد المصدر: تُبنى `tbCalcBalances` فوق `calcFSBalances` بدل تكرار منطق
مستقلّ — بعد حسم أيّ سلوك هو الصحيح محاسبياً.

---

## BUG-006 — `ensureStdAccount` غير Idempotent قبل تحديث `chartOfAccounts` 🔴

**الحالة:** موثّق (Phase 5) · **غير مُصلَح — الدالة نفسها لم تُعدَّل حرفاً واحداً في Phase 6.**
محروس بـ`tests/golden-master/idempotency.test.mjs`
**التوثيق الكامل:** `docs/accounting/ensureStdAccount.md`

**[Phase 6]** خدمة الترحيل الجديدة (`postPurchaseInvoice`) **لا تستدعي**
`ensureStdAccount` إطلاقاً — لأن `createJournalForPInv` الأصلية نفسها لا
تستدعيها (خلافاً لفاتورة المبيعات). هذا **سبب عدم تأثّر مسار المشتريات
بـBUG-006**، لا إصلاحاً للدالة. أي خدمة مستقبلية لفاتورة المبيعات ستواجه
BUG-006 كما هو تماماً — راجع `docs/services/posting.md` «التوصية لـPhase 7».

### الوصف
`ensureStdAccount(code)` تفحص `window.chartOfAccounts` الحالية فقط، بلا
قفل. استدعاءان متتاليان بنفس الحالة الناقصة (قبل أن يعكس `onValue` نتيجة
push الأول) ينشئان **حسابين قياسيين بنفس الرمز**.

### الأثر المالي
حساب مكرَّر في شجرة الحسابات بنفس الرمز — أي بحث لاحق عن الرمز قد يجد
أيّاً من النسختين، وقد تتوزّع الحركات على الاثنين بدل حساب واحد. يُفسد
شجرة الحسابات لا الدفتر مباشرةً، لكنه يُمهِّد لانحراف في `calcFSBalances`/
`tbCalcBalances` لاحقاً (حساب بنصف الحركات في كل نسخة).

### متى يقع فعلياً
عندما تحتاج فاتورة واحدة **حسابين قياسيين مفقودين معاً** (مثال: العملاء
1130 والإيرادات 4100 كلاهما غائبان)، أو عند نقر مزدوج على ترحيل فاتورتين
متتاليتين تحتاجان نفس الحساب القياسي المفقود قبل اكتمال أول جولة تحديث.

### لماذا لم يُصلَح هنا
يمسّ دالة تُستدعى من كل مسارات إنشاء القيود السبعة تقريباً. الإصلاح (قفل
تفاؤلي أو فحص مسبق داخل معاملة) يخضع لـ§6.

### **[Phase 7 Step C] — غير مُصلَح. لكن مسار المبيعات الجديد عزله بالكامل.**
`ensureStdAccount` **لم تُعدَّل حرفاً واحداً**، و`createJournalForSInv` القديمة
لا تزال تستدعيها حتى **ستّ مرّات** في الترحيل الواحد (1130/حساب المجموعة ·
4100/`salesAccountCode` · 2140 · 1131 · 2150) — فمسار المبيعات الحيّ هو أكثر
المسارات تعرّضاً لـBUG-006، ولا يزال كذلك بالكامل.

**الاكتشاف المؤكَّد في هذه المرحلة** (`docs/services/sales-invoice-discovery.md §4`):
الدالة تُعيد `rec` المحلّي بينما `window.chartOfAccounts` لا تتحدّث إلا بحدث
`onValue` لاحق ⇒ **الحساب المُنشأ غير متاح للنداء التالي داخل نفس الترحيل**،
وترحيلان متزامنان لشركة تنقصها 2140 يُنتجان حسابَي 2140.

**قرار Phase 7-C:** الخدمة الجديدة **لا تُنشئ حسابات إطلاقاً** — تحلّ الموجود
وترفض بـ`MissingAccountError`. لا مكرّرات لأن لا إنشاء أصلاً (فرق C1، مُختبَر
في `postSalesInvoice.failureInjection.test.mjs §[C]`: خمسة حسابات مفقودة ⇒
خمس حالات رفض + **صفر حساب مُنشأ**).

⚠️ الإصلاح الجذري (فهرس فرادة على `code`) **يتطلّب مخططاً جديداً** — خارج
صلاحية مرحلة الاستخلاص، ومسجَّل كقرار مالك معلّق (D1).

### الإصلاح المقترح
`runTransaction` على مسار محجوز (مثل `ledger/counters/stdAccount/${code}`)
قبل `push` — نفس نمط `BUG-002` المُعتمَد بالفعل في `saveCoaAccount`.

---

## BUG-007 — قيد مكرَّر يُظهر رصيداً خاطئاً في ميزان المراجعة بينما شاشة رصيد الطرف تبقى «صحيحة» 🔴

**الحالة:** موثّق (Phase 5) · غير مُصلَح · محروس بـ`tests/golden-master/idempotency.test.mjs`
و`tests/golden-master/failure-injection.test.mjs`
**يبني على:** Phase 4 §[B] في `posting-integrity.test.mjs` (اكتشاف: إرسال
مزدوج يُنتج قيدين) — هذا البند يوثّق **أين** يظهر أثره فعلياً وأين لا يظهر.

### الوصف
`calcVendorBalance`/`calcCustomerBalance` تُبنيان من **الفواتير** لا من
القيود (`docs/accounting/customer-balances.md`). قيد محاسبي مكرَّر (من
BUG سابق: إرسال مزدوج، أو قيد يتيم من فشل جزئي) **لا يظهر أثره** في شاشة
رصيد المورد/العميل — لأن مصدرها مستند الفاتورة نفسه، غير مضاعَف. لكنه
**يظهر بكامل قيمته** في `tbCalcBalances`/`coaAccountOps` (المبنيّتان من
القيود)، **متوازناً** (كلا القيدين سليم البناء على حدة) فلا يُطلق أي تنبيه.

### الأثر المالي
محاسب يفتح شاشة «أرصدة الموردين» فيرى رقماً صحيحاً تماماً، بينما ميزان
المراجعة لنفس المورد يحمل صمتاً ضعف المبلغ — **تناقض بين شاشتين بلا أي
تنبيه يربط بينهما**. هذا أخطر من مجرّد "قيد إضافي": الشاشة التي يراجعها
المحاسب عادة (رصيد المورد) لا تكشف المشكلة؛ ميزان المراجعة الذي يكشفها
نادراً ما يُقارَن سطراً بسطر بشاشة الأرصدة.

### السلوك الحالي (مُثبت باختبار)
`tests/golden-master/idempotency.test.mjs §[الأثر المالي المتباين]`: قيدان
بنفس `sourceKey` لفاتورة 11,500 ⇒ `tbCalcBalances` يُظهر 23,000 في حساب
الموردين (متوازن ✅)، بينما `calcVendorBalance` يُظهر 11,500 (صحيح، من
الفاتورة). نفس النمط في `failure-injection.test.mjs` للقيد اليتيم.

### لماذا لم يُصلَح هنا
الجذر هو BUG الترحيل غير الذرّي (`ACCOUNTING_INTEGRITY_FIX_PLAN.md §3/§5`)
— هذا البند إضافة توثيقية لأثره على التقارير، لا عطلاً مستقلاً يُصلَح
بمعزل عنه.

### **[Phase 6] — غير مُصلَح على المسار الحيّ. جذره مُغلَق في شفرة موازية غير موصولة.**
`postPInv`/`createJournalForPInv` القديمتان **لم تُعدَّلا حرفاً واحداً** —
لا تزالان غير ذرّيتين وغير Idempotent تماماً كما وثَّقت Phase 4، وBUG-007
**لا يزال قائماً بالكامل على مسار الإنتاج الفعلي.**

ما تغيّر: بُنيت خدمة موازية (`src/services/accounting/posting/postPurchaseInvoice.js`
+ `FirebaseJournalPostingRepository`) تُثبت أن الجذر (اللاذرّية + غياب
Idempotency) **قابل للحل معمارياً** بلا تغيير مخطّط ولا قواعد أمان —
مُختبَرة تنفيذياً بتزامن حقيقي (`Promise.all`، `tests/services/postPurchaseInvoice.idempotency.test.mjs`):
طلبان متزامنان حقيقيان على نفس الفاتورة ⇒ **قيد واحد فقط**، لا اثنان.
هذه الخدمة **غير موصولة بواجهة الإنتاج** (§15) — إثبات جدوى، لا إصلاح حيّ.
الوصل المُتحكَّم مرحلة منفصلة تتطلّب موافقة صريحة (Gate 3 المعادل).

### **[Phase 7 Step C] — غير مُصلَح على مسار المبيعات الحيّ. جذره مُغلَق في شفرة موازية.**
`postSInv`/`createJournalForSInv`/`createInventoryMovementsForSInv` القديمة
**لم تُعدَّل حرفاً واحداً**. الاكتشاف المؤكَّد: `postSInv` تفحص
`inv.status !== 'draft'` على **لقطة ذاكرة المتصفّح** بلا أي قفل، ثم تكتب
القيد والحالة وحركات المخزون في **3 + 2N عملية مستقلّة** — فتبويبان متزامنان
يُنتجان قيدين وحركتَي مخزون. BUG-007 على مسار المبيعات **أشدّ** منه على
المشتريات لأنه يضاعف المخزون أيضاً لا الدفتر فقط.

ما تغيّر: خدمة موازية (`src/services/accounting/posting/postSalesInvoice.js`
+ `FirebaseSalesInvoicePostingRepository`) تُثبت الحلّ بتزامن حقيقي — **2 و5
و10** طلبات متزامنة (`Promise.all`) على نفس الفاتورة ⇒ **ترحيل واحد بالضبط،
قيد واحد، مجموعة حركات واحدة**، ومجموع مدين دفتر اليومية بعد ترحيلين = 23,000
لا 46,000 (`tests/services/postSalesInvoice.idempotency.test.mjs §[3][4]`).
**غير موصولة بواجهة الإنتاج** — إثبات جدوى، لا إصلاح حيّ.

### الإصلاح المقترح
لا إصلاح مباشر — يُحلّ تلقائياً بحلّ الذرّية ومفتاح Idempotency في
`ACCOUNTING_INTEGRITY_FIX_PLAN.md §3/§5`. الإضافة المقترحة هنا: **فحص
اتّساق دوري** يقارن مجموع حركات كل طرف في دفتر الأستاذ برصيده المحسوب من
الفواتير، وينبّه عند الفرق — شبكة أمان إضافية مستقلّة عن إصلاح الجذر.

---

## BUG-008 — `allocateToInvoices` تقبل تخصيص سند يتجاوز رصيد الفاتورة المتبقّي بلا أي حدّ 🔴

**الحالة:** موثّق (Phase 7 Step B) · غير مُصلَح على المسار الحيّ · محروس
بـ`tests/characterization/allocateToInvoices.test.mjs`

### الوصف
`allocateToInvoices` (accounting.js:19731):
```js
const newPaid = Math.round((currentPaid + parseFloat(amt)) * 100) / 100;
await update(ref(db, invPath + invKey), { paidAmount: newPaid });
```
**لا فحص `newPaid > grandTotal` في أي مكان.** لا خطأ، لا `toast`، حتى لا
`console.warn`. أي سند قبض/صرف — عبر إدخال يدوي أو سباق حقيقي بين سندين
متزامنين على نفس الفاتورة — يمكن أن يدفع `paidAmount` إلى ما يتجاوز
`grandTotal` بلا أي رفض.

### السلوك الحالي (مُثبت باختبار)
`tests/characterization/allocateToInvoices.test.mjs`: تخصيص 6000 ثم 7000
إضافية على فاتورة قيمتها 10000 ⇒ `paidAmount: 13000`، `error: null`،
`toasts: []`. أيضاً: فاتورة تخصيص بمفتاح غير موجود تُتجاوَز بـ`console.warn`
فقط (accounting.js:19740) — بقيّة التخصيصات تتابع بصمت.

### الأثر المالي
فاتورة تظهر «مسدَّدة بالزيادة» بلا أي تنبيه — يُفسد حسابات الرصيد المتبقّي
والمتأخرات لهذا العميل/المورد. في سباق حقيقي (نقرتان على ترحيل سندين
مختلفين على نفس الفاتورة بفارق أجزاء من الثانية) يمكن أن يتضاعف الخطأ لأن
كلا الطلبين يقرآن `paidAmount` قبل أن يكتب أيّهما — لا قفل ولا معاملة.

### لماذا لم يُصلَح هنا
يمسّ `allocateToInvoices` القديمة — دالة كتابة مباشرة على المسار الحيّ.
الإصلاح (فحص + رفض) يغيّر سلوكاً قائماً (تخصيصات كانت تُقبَل ستُرفَض) ويحتاج
موافقة صريحة + مراجعة بيانات الإنتاج لفواتير مُخصَّصة بالفعل بأكثر من قيمتها.

### **[Phase 7 Step B] — غير مُصلَح على المسار الحيّ. جذره مُغلَق في شفرة موازية غير موصولة.**
`postVoucher`/`allocateToInvoices` القديمتان **لم تُعدَّلا حرفاً واحداً** —
لا تزالان تقبلان التجاوز بلا حدّ تماماً كما وثَّقت هذه المرحلة، وBUG-008
**لا يزال قائماً بالكامل على مسار الإنتاج الفعلي.**

ما تغيّر: بُنيت خدمة موازية (`src/services/accounting/posting/postVoucher.js`
+ `FirebaseVoucherPostingRepository` + `computeInvoiceAllocation`) تثبت أن
رفض التجاوز **قابل للتنفيذ معمارياً وآمن من التزامن فعلياً** بلا تغيير مخطّط
ولا قواعد أمان — مُختبَرة تنفيذياً بسباق حقيقي (`Promise.all`،
`tests/services/postVoucher.allocation.test.mjs` «السباق الحقيقي»): سندان
متزامنان (6000 و7000) على فاتورة رصيدها 10000 ⇒ **واحد فقط ينجح**، لا كلاهما.
هذه الخدمة **غير موصولة بواجهة الإنتاج** — إثبات جدوى، لا إصلاح حيّ. راجع
`docs/services/voucher-allocation.md` للتفصيل الكامل وتصنيف الفرق (C).

### الإصلاح المقترح
نفس نمط BUG-002/BUG-006 المُعتمَد: استبدال `get` ثم `update` منفصلتين بـ
`runTransaction` على عقدة الفاتورة، مع فحص `newPaid > grandTotal` **داخل**
دالة المعاملة — بالضبط ما تفعله `computeInvoiceAllocation` +
`FirebaseVoucherPostingRepository` في الخدمة الموازية. يتطلّب موافقة صريحة
قبل التطبيق على المسار الحيّ (§6).

---

## BUG-009 — `createInventoryMovementsForSInv` تبتلع فشل كل حركة على حدة فيبقى المخزون ناقصاً بصمت 🔴

**الحالة:** مُكتشَف ومُوثَّق (Phase 7 Step C) · **غير مُصلَح — الدالة لم تُعدَّل حرفاً واحداً.**
**التوثيق الكامل:** `docs/services/sales-invoice-inventory.md` · `docs/services/sales-invoice-discovery.md §6`

### الوصف
`createInventoryMovementsForSInv` (accounting.js:21670) تلفّ إنشاء **كل حركة على
حدة** في `try/catch` لا يفعل شيئاً سوى `console.error`:

```js
try { … await push(R.invmov, {…}); count++; }
catch (e) { console.error('Create inventory movement (sale) error:', e); }
```

فشل حركة واحدة من ثلاث ⇒ الفاتورة تبقى **مرحّلة**، القيد المحاسبي **مكتوب**،
وحركتان فقط من ثلاث في المخزون — **بلا أي تنبيه للمستخدم**. الرسالة الوحيدة
التي يراها هي `📦 تم إنشاء {count} حركة` بالعدد الناقص، ولا شيء يقول إن العدد
كان يجب أن يكون أكبر.

### الأثر المالي
رصيد الصنف في النظام أعلى من الواقع بمقدار الكمّية المفقودة، دائماً وإلى الأبد
ما لم يُكتشف بجرد يدوي. ولأن تقييم المخزون يُبنى من نفس الحركات
(`calcInvItemMovingAvg`)، فقيمة المخزون في القوائم المالية مُضخَّمة كذلك.
وهذا يقع **بعد** كتابة القيد، فلا الميزان ولا رصيد العميل يُظهر أي خلل يشير إليه.

### متى يقع فعلياً
انقطاع اتصال أو رفض صلاحية أثناء الحلقة (كل حركة كتابة شبكية مستقلّة + معاملة
عدّاد مستقلّة)، أو فشل معاملة العدّاد لصنف واحد. احتمال الوقوع يتناسب طردياً مع
عدد أصناف الفاتورة.

### لماذا لم يُصلَح هنا
تعديل `public/accounting.js` ممنوع صراحةً في مرحلة الاستخلاص (§18).

### **[Phase 7 Step C] — الجذر مُغلَق في الشفرة الموازية**
`FirebaseSalesInvoicePostingRepository` تكتب **كل** الحركات مع القيد والربط في
`update` واحد متعدّد المسارات: إمّا الكلّ أو لا شيء. لا حركة ناقصة، ولا ابتلاع
خطأ — مُختبَر في `postSalesInvoice.inventory.test.mjs §[6]` و
`postSalesInvoice.atomicity.test.mjs §[2]` (فشل ⇒ صفر حركات من اثنتين، لا واحدة).

### الإصلاح المقترح
استبدال حلقة `push` المستقلّة بتحديث ذرّي واحد يضمّ الحركات كلها — بالضبط ما
تفعله الخدمة الموازية. يتطلّب موافقة صريحة قبل التطبيق على المسار الحيّ (§6).

---

## BUG-010 — `submitCreditNote`/`submitDebitNote` لا تفحصان نتيجة بناء القيد فيصدر الإشعار بلا قيد 🔴

**الحالة:** مُكتشَف ومُوثَّق (Phase 7 Step D) · **غير مُصلَح — الدوال لم تُعدَّل بحرف.**
**التوثيق:** `docs/services/credit-note-discovery.md §3` · مُثبَت تشغيلياً في
`tests/golden-master/credit-note.test.mjs §[6]` و`debit-note.test.mjs §[7]`

### الوصف
`createJournalForCreditNote` (accounting.js:16133) عند غياب حساب العملاء أو الإيرادات:
```js
if (!receivableAcc || !revAcc) { toast('⚠️ الإشعار سُجّل لكن تعذّر إنشاء القيد…', 'wn', 8000); return; }
```
تعود **بلا قيمة**، و`submitCreditNote` (:16112) تستدعيها بلا أي فحص:
```js
await createJournalForCreditNote(cnRef.key, cnData);   // ← لا فحص
await createReturnMovementsForCN(cnRef.key, cnData);
… await update(ref(db, 'ledger/salesInvoices/' + key), upd);   // creditedAmount / fullyCredited
```
نفس البنية حرفياً في `submitDebitNote` (:16249). يُخالف انضباط `postSInv` التي تفحص
`if (!jrnRef) … return;` قبل تثبيت الترحيل.

### السلوك الفعلي (مُثبت بتشغيل الدالة الحقيقية)
شجرة حسابات ناقصة ⇒ **صفر قيد · صفر استثناء**، ومع ذلك: المستند كُتب · حركتا مخزون
كُتبتا · الفاتورة عُلِّمت `fullyCredited: true` · وعُرضت للمستخدم رسالة **نجاح**
«✅ صدر الإشعار الدائن …» إلى جانب تحذير `wn` عابر.

### الأثر المالي
`calcCustomerBalance` (:12280) تُنقص الرصيد بـ`creditedAmount` وتستبعد الفاتورة كلياً عند
`fullyCredited`، بينما دفتر الأستاذ لم يتغيّر ⇒ **رصيد العميل يُظهر الخصم وميزان
المراجعة لا يُظهره**. انفصال بين شاشتين بلا أي تنبيه يربطهما — نفس عائلة BUG-007.
والمخزون يُعاد فعلياً بلا أي أثر محاسبي مقابل.

### لماذا لم يُصلَح
تعديل `public/accounting.js` ممنوع صراحةً في مرحلة الاستخلاص (§1).

### **[Phase 7 Step D] — الخدمة الموازية لا تُعيد إنتاجه**
`postCreditNote`/`postDebitNote` ترفضان العملية بـ`MissingAccountError` **قبل** أي كتابة
مالية — لا مستند ولا مخزون ولا تحديث فاتورة. مُختبَر لثلاثة حسابات في كل مسار
(`postCreditNote.failure.test.mjs §[B]`). ⚠️ **ليس إصلاحاً**: المسار الحيّ كما هو.

### الإصلاح المقترح
فحص القيمة المعادة قبل كتابة المخزون وتحديث الفاتورة — نفس نمط `postSInv` المُعتمَد
بالفعل. يتطلّب موافقة صريحة قبل التطبيق على المسار الحيّ.

---

## BUG-011 — ترقيم الإشعارات `max(cache)+1` بلا معاملة ⇒ أرقام مكرَّرة ومُعاد استخدامها 🔴

**الحالة:** مُكتشَف ومُوثَّق (Phase 7 Step D) · **غير مُصلَح.**
**التوثيق:** `docs/services/credit-note-discovery.md §4` · مُثبَت في
`tests/characterization/creditNote.test.mjs §[1]` و`debitNote.test.mjs §[1]`

### الوصف
`generateCNNumber` (:16037) و`generateDNNumber` (:16175) تشتقّان الرقم من
`window.creditNotes`/`window.debitNotes` — **لقطة متصفّح** لا تتحدّث إلا بحدث `onValue`
لاحق، وبلا أي معاملة.

### السلوك الفعلي (مُثبت بتشغيل الدالتين الحقيقيتين)
| الحالة | الناتج |
|---|---|
| نداءان قبل تحديث اللقطة | **رقمان متطابقان** (`CN-2026-00002` مرّتين) |
| حُذف أعلى إشعار (بقي 1 من 1,5) | `CN-2026-00002` — **رقم قد يكون مستعملاً** |
| رقم مشوّه أو بلا `number` | يُعامَل كصفر (لا ينهار) |
| فجوة (1,5) | `00006` — الفجوات لا تُملأ |
| سنة سابقة | معزولة سليمة |

### الأثر المالي
مستندان ضريبيان بنفس الرقم — مخالفة لمتطلّبات الفوترة، وإرباك في المطابقة والتقارير.
لا يُفسد الأرصدة مباشرةً، لكنه يُفسد **هوية المستند**.

### **[Phase 7 Step D] — الخدمة الموازية لا تُعيد إنتاجه**
عدّاد معامِلاتي على `ledger/counters/{cn|dn}/{year}` (**قرار المالك رقم 2**) — داخل
مجموعة `counters` القائمة، بلا مجموعة جديدة. خمسة إشعارات متزامنة ⇒ خمسة أرقام فريدة
(`char §[2]`). الفجوات مقبولة صراحةً؛ التكرار لا.

### الإصلاح المقترح
استبدال `max(cache)+1` بـ`runTransaction` على عدّاد — نفس ما تفعله الخدمة الموازية،
ونفس نمط `generateJrnNumberAtomic` المُعتمَد بالفعل في نفس الملف.

---

## BUG-012 — تحديث الفاتورة المصدر قراءة-ثم-كتابة من اللقطة ⇒ ضياع تحديث 🔴

**الحالة:** مُكتشَف ومُوثَّق (Phase 7 Step D) · **غير مُصلَح.**

### الوصف
```js
const prevCredited = parseFloat(inv.creditedAmount) || 0;              // من لقطة الذاكرة
const keys = Array.isArray(inv.creditNoteKeys) ? inv.creditNoteKeys.slice() : …;
keys.push(cnRef.key); upd.creditNoteKeys = keys;
upd.creditedAmount = Math.round((prevCredited + c.grandTotal) * 100) / 100;
await update(ref(db, 'ledger/salesInvoices/' + key), upd);
```
إشعاران متزامنان يقرآن `prevCredited` نفسه ويكتبان فوق بعضهما.

### الأثر المالي
`creditedAmount` يعكس إشعاراً واحداً فقط بينما صدر إشعاران ⇒ **رصيد العميل أعلى من
الواقع**، ومفتاح إشعار يختفي من `creditNoteKeys` فيصعب تتبّعه. نفس البنية في المسار المدين.

### **[Phase 7 Step D] — الخدمة الموازية لا تُعيد إنتاجه**
التحديث يقع داخل `runTransaction` على عقدة الفاتورة، والحساب يتمّ **داخل** المعاملة عبر
`computeNoteCapacity`. إشعاران متزامنان (1725 + 575) ⇒ المجموع 2300 بالضبط والمفتاحان
محفوظان (`idempotency §[4]` · `allocation §[4]`).

### الإصلاح المقترح
`runTransaction` على عقدة الفاتورة بدل `get`-ثم-`update` — نفس نمط BUG-008 المقترح سابقاً.

---

## BUG-013 — الإشعار الثاني يُحسب على الفاتورة الأصلية لا المتبقّي ⇒ تجاوز في الدفتر وذمّة سالبة 🔴

**الحالة:** مُكتشَف ومُوثَّق (Phase 7 Step D) · **غير مُصلَح.**
**مُثبَت تشغيلياً:** `tests/golden-master/credit-note.test.mjs §[6] 41` · `debit-note §[7] 37`

### الوصف
`cnCompute` تحدّ كل سطر بكمّيته **الأصلية**، ولا تعرف شيئاً عن `creditedAmount` السابق.
والحارس الوحيد `!fullyCredited` لا يمنع إشعاراً جزئياً ثانياً بكامل الكميات.

### السلوك الفعلي (مُثبت)
```
فاتورة 2300 → إشعار جزئي 1380  ⇒ creditedAmount = 1380
             → إشعار ثانٍ بكامل الكميات = 2300
                isFull = (1380 + 2300) ≥ 2300 − 0.01  ⇒ صحيح
                ⇒ creditedAmount = 2300  (مقصوص!) · fullyCredited = true
                ⇒ لكن القيد يعكس 2300 كاملة
الأثر الدفتري: 2300 مدين − 1380 − 2300 = **ذمّة عميل سالبة 1380**
```

### الأثر المالي
**الحقل مقصوص والدفتر متجاوَز** — انفصال ثالث بين شاشة رصيد الطرف وميزان المراجعة،
وإيراد مُخفَّض بأكثر ممّا سُجِّل أصلاً. نفس البنية في المسار المدين (`debitedAmount`).

### **[Phase 7 Step D] — الخدمة الموازية لا تُعيد إنتاجه**
`computeNoteCapacity` (قرار المالك رقم 3) تحسب المتبقّي من الحالة اللحظية للفاتورة
**داخل معاملة** وترفض التجاوز بـ`AllocationConflictError`. السباق الحقيقي عند 2 و5 و10
إشعارات متزامنة ⇒ **ناجح واحد بالضبط** والباقي تعارض مُصنَّف (`allocation §[1][3]`).

### الإصلاح المقترح
حساب المتبقّي من `grandTotal − creditedAmount` داخل معاملة قبل السماح بالإشعار.

---

## BUG-014 — `calcCustomerBalance` تستبعد الفاتورة المُلغاة بالكامل بما فيها `paidAmount` 🔴

**الحالة:** مُكتشَف ومُوثَّق (Phase 7 Step D) · **غير مُصلَح — وتوصيف فقط بأمر صريح.**
**التوثيق:** `docs/services/debit-note-posting.md §5`

### الوصف
```js
// calcCustomerBalance — accounting.js:12280
if (inv.fullyCredited) return;   // ← تستبعد الفاتورة كاملةً، بما فيها paidAmount
```
بينما `calcVendorBalance` (:17069) **لا يفحص `fullyDebited` إطلاقاً**؛ يطرح
`debitedAmount` من الإجمالي فقط فيبقى `paidAmount` محسوباً.

### الأثر المالي
| السيناريو | العميل | المورد |
|---|---|---|
| فاتورة 10,000 · مدفوعة 10,000 · إشعار كامل | مساهمة **0** | مساهمة **−10,000** |
| الصواب المحاسبي | −10,000 | −10,000 ✅ |

⇒ مسار العميل **يُخفي التزاماً بقيمة ما دفعه العميل** على فاتورة أُلغيت بإشعار دائن كامل
(مبلغ واجب الردّ). ولا شيء يمنع الحالة: `openCreditNote` لا يفحص `paidAmount` إطلاقاً،
بخلاف `unpostSInv` التي ترفض إن `paid > 0.005`.

### السلوك الفعلي — **مُثبَت بتشغيل الدالتين الحقيقيتين**
`npm run test:gm:bug014` (`tests/golden-master/bug014-note-balance-asymmetry.test.mjs`،
22 تأكيداً) يُشغّل `calcCustomerBalance` و`calcVendorBalance` من الملف الحيّ عبر مِشجب
Phase 5، على مدخلات متطابقة تماماً:

```
فاتورة 10,000 · مدفوعة 10,000 · إشعار كامل 10,000
  العميل : invoiced=0  paid=0      credited=0  ⇒ balance =        0
  المورد : invoiced=0  paid=10,000             ⇒ balance =  −10,000
```

**جذر العطل مُعزول بالاختبار:** استبعاد الفاتورة كلياً يُسقط `paidAmount` من الحساب
(`paid=0` عند العميل مقابل `paid=10,000` عند المورد). وأُثبت أيضاً:
- بلا دفع مسبق ⇒ **الطرفان يتطابقان** (0 و0) — العطل مشروط بوجود مدفوع.
- في الإشعار **الجزئي** ⇒ الطرفان متطابقان (−4,000 لكليهما) — العطل حصريّ في
  مسار `fullyCredited`.
- **الفارق = المبلغ المدفوع بالضبط** عند 0 · 2,500 · 5,000 · 7,500 · 10,000.
- `openCreditNote` **لا يفحص `paidAmount` إطلاقاً**، بينما `unpostSInv` ترفض إن
  `paid > 0.005` — الحارس موجود هناك وغائب هنا.

### التقارير المتأثّرة
شاشة أرصدة العملاء · أعمار الذمم · التدفّق النقدي المتوقّع (`finmodels.js:75`) ·
أي تقرير يبني على `calcCustomerBalance`.

### لماذا لم يُصلَح
**أمر صريح في تعليمات Phase 7 Step D §17: توصيف فقط، بلا أي تعديل إنتاجي.**
والخدمة الموازية **لا تعالجه** — الحسابان (`calcCustomerBalance`/`calcVendorBalance`)
خارج نطاق خدمة الإصدار تماماً.

### الإصلاح المقترح (لمرحلة لاحقة)
توحيد المعالجتين: طرح `creditedAmount` من الإجمالي بدل استبعاد الفاتورة — أي جعل مسار
العميل يطابق منطق المورد. يتطلّب قراراً محاسبياً وموافقة صريحة.

---

## BUG-015 — نوع تكلفة مشروع مخصّص (`custom_*`) يُسجَّل بلا قيد محاسبي إطلاقاً 🔴

**الحالة:** مُكتشَف ومُوثَّق (Phase 7 Step E) · **غير مُصلَح — الشفرة القديمة لم تُلمَس.**
**مُثبَت تشغيلياً:** `tests/golden-master/project-cost.test.mjs §[3]`

### الوصف
`getPMCCategoryInfo` (accounting.js:7591) تُرجع للأنواع القياسية كائناً يحمل
`defaultDebitAccountCode`/`defaultCreditAccountCode`، لكنها تُرجع للأنواع **المخصّصة**
(`custom_*`) وللنوع غير المعروف كائناً بـ`{ar, color, bg}` فقط — **بلا أي رموز حسابات**:

```js
if (PMC_CATEGORIES[cat]) return PMC_CATEGORIES[cat];      // ← يحمل الحسابات
if (rawKey && custom[rawKey]) return { ar: …, color: …, bg: … };   // ← لا يحملها
return { ar: cat || '—', color: '#888', bg: '#f0f5fa' };           // ← ولا هذا
```

فتقرأ `createJournalForPMC` رمزاً `undefined`، ولا تجد الحساب، فتعرض `toast` تحذيرياً
وتعيد `null`. والمستدعي `savePMC` (:10281) يفحص `if (jrnResult)` فيتخطّى الربط **بصمت**.

### السلوك الفعلي (مُثبت بتشغيل الدالة الحقيقية)
```
نوع 'custom_steel'  ⇒  createJournalForPMC → null · صفر قيد · toast('wn') فقط
نوع 'ghost'         ⇒  نفس النتيجة تماماً
```
والتكلفة **تبقى مكتوبة** في `ledger/projectMonthlyCosts` بلا `journalEntryKey`.

### الأثر المالي
تكلفة مشروع مُسجَّلة في تقارير المشروع بينما **دفتر الأستاذ لا يعرف عنها شيئاً** —
مصروف ناقص في قائمة الدخل، والتزام ناقص تجاه المورد. المستخدم يرى «✅ تم إضافة التكلفة»
ولا شيء يشير إلى غياب القيد سوى تنبيه عابر. نفس عائلة BUG-010.

### متى يقع فعلياً
عند اختيار نوع تكلفة مخصّص محفوظ في Firebase (`categorySelect.startsWith('custom_')`).
⚠️ لاحظ أن المسار `'__custom__'` (نوع جديد يُكتب يدوياً) يُسنِد `category = 'other'`
وهو نوع **قياسي يحمل حسابات** — فلا يقع العطل هناك. العطل حصريّ في الأنواع المخصّصة
**المحفوظة**.

### لماذا لم يُصلَح
تعديل `public/accounting.js` ممنوع في هذه المرحلة (Option A).

### **[Phase 7 Step E] — الخدمة الموازية لا تُعيد إنتاجه**
`postProjectMonthlyCost` تفحص `pmcCategoryHasAccounts` وترفض بـ`MissingAccountError`
**قبل أي كتابة** — لا تكلفة بلا قيد. مُثبَت في
`tests/services/postProjectMonthlyCost.test.mjs §[4] D`. ⚠️ **ليس إصلاحاً**: المسار
الحيّ كما هو.

### الإصلاح المقترح
إمّا إضافة حقلَي حساب لكل نوع مخصّص في شاشة إدارة الأنواع، وإمّا الرجوع الصريح إلى
حسابات `other` (`5390`/`2110`) عند غيابهما. **قرار مالك** — الخياران يغيّران التصنيف
المحاسبي للتكلفة.
