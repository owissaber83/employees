# PRODUCTION_ISSUES.md

> مشكلات تمسّ الإنتاج الحيّ، **خارج نطاق مشروع الترحيل**. تُرفع لقرار مستقل.

---

## PROD-001 — بوابة المشروع معطّلة في الإنتاج

### التصنيف: **P1** (لا P0)

**لماذا P1 لا P0:** لا خسارة بيانات ولا ثغرة أمنية ولا تعطّل للنظام الأساسي.
ميزة واحدة لا تعمل. لكنها **P1 لا P2** لأنها تفشل أمام طرف خارجي (عميل المشروع)
برسالة تقنية، وهو ما يمسّ سمعة النظام أمام عملاء شركتك.

### الدليل المقيس

| | |
|---|---|
| إشارات `portal` في `database.rules.json` | **صفر** |
| إشارات `portalSnapshots`/`portalResponses` في الواجهة | **9** (`app.js` · `project-detail.js`) |
| مسار الدخول | `#portal=<token>` مُفعَّل في `app.js:1553` |
| اختبارات القواعد الفاشلة | **5** من 315 |

قواعد أمان البوابة **لم تُكتب قط**، بينما الواجهة تعتمد عليها.

### الأثر
الزائر الذي يفتح رابط البوابة يتلقّى `permission_denied`. ولا يستطيع:
قراءة لقطة المشروع · اعتماد مستخلص · الإجابة على RFI.
وموظفوك لا يستطيعون إنشاء لقطة بوابة أو قراءة صندوق الردود.

### 🛡️ لماذا هذه ليست ثغرة أمنية — Fail Closed

نموذج أمان Firebase RTDB **يمنع افتراضياً**: ما لا تسمح به قاعدة صريحة فهو ممنوع.
وبما أن القواعد لا تذكر `portal` إطلاقاً، فكل مسارات البوابة **مرفوضة للجميع**،
بمن فيهم غير المصادَقين.

**الدليل القاطع من الاختبارات نفسها:** الاختبارات الستّة من نوع `assertFails`
— وهي اختبارات **الاختراق** — **تمرّ كلها**:
- «زائر لا يُنشئ لقطة بوابة (تزوير)» ✅ يمرّ
- «زائر لا يُلحق ردّاً بإجراء غير مسموح» ✅ يمرّ

أي أن الخمسة الفاشلة كلها من نوع `assertSucceeds` — وظائف **مشروعة** لا تعمل.
النظام يرفض الجميع بلا تمييز: **فشل مغلق (Fail Closed)، لا باب مفتوح.**

### الحالة المطلوبة قبل الإصلاح
كتابة قواعد `portalSnapshots` و`portalResponses` تسمح بالمشروع وتمنع التزوير:
قراءة اللقطة بالرمز فقط · إلحاق ردّ بإجراءات محدّدة (`approve`/`note`/`answer`) ·
منع الزائر من إنشاء اللقطات أو تعديل القائم · احترام `revoked`.

### لماذا لم يُعالَج في Phase 2
تغيير `database.rules.json` يخضع لشرطك الخامس (توقّف واطلب موافقة)، وليس
ضرورياً لعمل Phase 2. **ينتظر قرارك كبند مستقل.**

### تنبيه على النشر
`.github/workflows/firebase-hosting-merge.yml` ينشر `public/` فقط.
**قواعد قاعدة البيانات لا تُنشَر عبر CI إطلاقاً** — تحتاج
`firebase deploy --only database` يدوياً.

---

## PROD-002 — لا نسخ احتياطي خادمي

### التصنيف: **P0**

`functions/dailyBackup` **غير منشورة** (خطة Spark تمنع Cloud Functions).
البديل الوحيد `downloadLocalBackup` يعمل **فقط حين يفتح مدير التطبيق** ذلك اليوم.

**الأثر:** لا نسخة مضمونة من بيانات مالية ورواتب حيّة. يوم لا يفتح فيه مديرٌ
التطبيق هو يوم بلا نسخة.

**لماذا P0:** كل بند آخر يمسّ ميزة؛ هذا يمسّ **بقاء البيانات نفسها**.

**الخيارات:** ترقية إلى Blaze ونشر `dailyBackup` · أو تصدير مجدول من خارج
التطبيق (سكربت على جهاز/خادم بمفتاح خدمة) · أو `scripts/backup.sh` القائم مجدولاً.

**خارج نطاق الترحيل** — لكنه أخطر بند في هذه الوثيقة.
