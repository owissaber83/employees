# 🤝 HANDOFF — تسليم العمل (جلسة 2026-08-09)

> ملخّص تنفيذي لكل ما أُنجز، وما بقي، وما اكتُشف — كي يُكمل عليه أي جهاز/جلسة لاحقة.
> مشروع Firebase: `emplyeeapp-1dc64` · خطة **Spark المجانية** (لا Cloud Functions).

---

## 0) الحالة عند التسليم

| البند | الحالة |
|---|---|
| كل العمل في git | ✅ مدفوع إلى `origin/main` |
| الاستضافة | ✅ منشورة (auto-deploy عند push) |
| قواعد قاعدة البيانات | ✅ **منشورة** (القوى العاملة + قاعدة أمان المشاريع) — 2026-08-09 |
| اختبارات قواعد الأمان | ✅ **197 ناجح · 0 فاشل** |

> **🆕 تحديث جلسة 2026-08-09 (متابعة الـ handoff):** نُفِّذت ونُشِرت بنود Priority 1:
> **(§5-1)** نشر قواعد القوى العاملة (فُكّ حظر الإنتاج). **(§5-3أ)** إصلاح ازدواج خصم الدفعة المقدمة — حُذف طرح `advanceAmount` من صافي المستخلص (27084)، وبند «استرداد الدفعة» في القيد صار المتبقّي الموازِن (`cur+vat−net−ret−oth`) فيبقى القيد متوازناً للمستخلصات القديمة والجديدة، وأُخفي حقل «دفعة مقدمة %». **(§5-3ب)** قاعدة أمان `projects` (كتابة/حذف لـ admin/project_manager/executive_director فقط؛ القراءة تبقى للأعضاء — سرّية قيمة العقد follow-up) + حرّاس `savePrj`/`delPrj`. **(§5-3ج)** حذف ناعم: `delPrj` يؤرشف (`archived:true`) بدل الحذف الصلب + `restoreProject` + تبديل «المؤرشفة» في `renderProjects` + استثناؤها من القوائم المنسدلة.
> **(§5-3ب+)** سرّية مالية المشاريع — أُغلقت ثغرة «قراءة قيم كل العقود»: غلاف `watch` على 11 مستمع مشاريع مالي، وقواعد **قراءة = غير موظف/غير مشاهد**. **(§5-3ب++)** تصليب **الكتابة** بالأدوار (بتأكيد سير عمل المستخدم): `projectBOQ`→admin/PM؛ `progressBillings`→admin/PM/accountant/site_engineer/executive؛ `subcontracts/subcontractorTrans/projectExpenses`→+purchasing/procurement؛ `projectChangeOrders/PaymentSchedule/Markups/MonthlyCosts`→admin/PM/accountant/executive. **المحاسب مشمول في المستخلصات عمداً** (يُرحّل القيد/يخصّص المقبوضات — استبعاده يكسر التدفّق). يُغلق كتابة HR/إداري/مخزن/مستلم. الاختبارات = **225 ناجح · 0 فاشل**.
> **(مراجعة المشاريع §4 — Priority 2/3):** أُصلحت: تعارض الحالات (خريطة `PRJ_STATUS` موحّدة للحالات السبع + إكمال الفلتر)؛ أداء `renderProjects` (فهرسة مسبقة لعدد الموظفين ومجاميع المستخلصات بدل O(N×B))؛ ترقيم صفحات (50/صفحة) + توسيع البحث (كود/عميل/رقم عقد)؛ تحقّق مدخلات `savePrj` (تاريخ معكوس/سالب/نِسب>100)؛ حراسة دورة الحياة (تأكيد تعديل مشروع مكتمل/ملغى)؛ حذف الكود الميت (clientName/Address/VAT المكرّرة).
> **ما زال مفتوحاً:** معاينة بشرية بتسجيل دخول؛ ثيم جسر في `app.js`؛ فارق `today()` (حضور/رواتب).

**⚠️ تنبيه دائم:** GitHub Actions ينشر **الاستضافة فقط**. بعد أي تعديل على `database.rules.json`:
```bash
export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"   # Java مطلوب للمحاكي
npm run test:rules
firebase deploy --only database
```

---

## 1) 🎨 ثيم «جسر» لقسم الموارد البشرية (مُنجز ومنشور)

هوية بصرية معزولة تسري على **31 صفحة HR فقط**؛ بقية البرنامج يبقى بالهوية الكحلية.

- **المرحلة أ** (`e70f808`): تحويل **453 لوناً صريحاً** في `hr-suite/hr-requests/hr-extra/hr-approvals` إلى **23 رمزاً دلالياً** (`--hr-pri`, `--hr-acc`, `--hr-sf1`…) معرّفة في `styles.css`. صفر تغيير بصري في هذه المرحلة.
- **المرحلتان ب+ج** (`5678dbb`): `HR_SECTION_PAGES` (31 صفحة) في `app.js` تضبط `body[data-sec="hr"]` داخل `nav()`، واللوحة معرّفة تحت هذا المحدِّد في `styles.css`.

### مبدأ التصميم — مهم قبل أي تعديل
فحص الاستخدام أثبت أن `--hr-pri` لون **نصوص** (20 موضعاً) لا خلفية، وأن **الأصفر كنص على أبيض تباينه 1.44:1** (غير مقروء). فالتوزيع حسب الدور:
- **أصفر جسر `#ffd24d`** → أسطح فقط: الشريط العلوي، عناوين البطاقات، رأس الجدول، الزرّ الأساسي، تمييز الصف
- **`#8a6100` ذهبي** → النسخة النصّية الآمنة من الأصفر (`--hr-acc`)
- **`#1c2b3f`** → كل النصوص · **`#2b72bd`** → العناصر التفاعلية

كل ألوان النصوص **≥ 4.5:1 (WCAG AA)** — تحسّن على السابق (`--hr-muted` 2.56→4.85 · `--hr-acc` 3.28→5.54).

### ⚠️ استثناءات لا تلمسها
1. **`hsOpenLetterWindow`** (`hr-suite.js` ~199-256): يكتب خطاب HR في **نافذة مستقلة لا ترث متغيّرات CSS**. ألوانه صريحة عمداً — تحويلها يُخرج الخطابات بلا ألوان.
2. **لاحقة الشفافية السداسية**: النمط `background:${color}18` يفسد مع `var()`. حُوّل إلى `color-mix(in srgb, ${c} 9.41%, transparent)` (9.41% = 24/255 بالضبط). لا تُعده.

### ما زال ناقصاً
صفحات HR داخل **`app.js`** (الموظفون، الحضور، الرواتب، الإجازات) ألوانها ما زالت صريحة — تأخذ الأنماط المشتركة فقط (`.card`, `.c-tl`, الجداول, الأزرار, الشريط العلوي)، فهويتها **جزئية**. إتمامها = المهمة «أكمل app.js».

---

## 2) 🕐 قسم الحضور (مُنجز ومنشور)

### أ) بحث وحذف (`22a1c2d`)
- **بحث** فوق قائمة التحضير الشهري (`atFilterBulkEmps`) — يُخفي غير المطابق **ولا يلغي تحديده**، و«تحديد الكل» يعمل على **المعروض فقط**.
- **`ssEnhance`** (أداة القوائم المنسدلة بالبحث الموجودة في `accounting.js`) طُبِّقت على `atAdminEmp` · `atFilterEmp` · `atPurgeEmp` + `ssRefresh` عند تحديث الخيارات.
- **حذف التحضير الشهري** (بطاقة `atPurgeCard`): شهر + موظف/الكل، معاينة العدد قبل التنفيذ، تحذير إن وُجد مسير رواتب على نفس الشهر، حارس `manage_attendance`، تسجيل في `logAudit`.
- **سلوك وقائي:** التحضير الجماعي يوسم سجلاته `bulkGenerated:true` بينما تسجيلات الموظف الحقيقية لا تُوسم. فخيار **«التحضير الجماعي فقط» مفعّل افتراضياً** كي لا يمسح الحذف بصمات حضور فعلية.
- حذف اليوم المفرد **موجود أصلاً** — `deleteAtRecord` (زر 🗑️ في كل صف). لم يُبنَ بديل.

### ب) تبويبات الصفحة (`7c41db3`)
ثلاثة تبويبات بدل تمرير طويل، **بلا عنصر شريط جديد** (الشريط فيه 146 عنصراً): 🕐 اليوم · 📋 السجلات · 📆 التحضير الشهري (للمسؤول). نمط `window._atTab` (نفس `_essTab` في الخدمة الذاتية). الموظف العادي يرى تبويبين.

**مخالفتان مقصودتان:** «من الحاضر الآن» و«طلبات التصحيح» بقيتا في تبويب «اليوم» — الأولى لوحة لحظية لا سجل، والثانية طابور موافقات لا تقرير.

### ج) تقويم الإجازات الرسمية (`1628c08`)
كان **خللاً في الاحتساب لا ميزة ناقصة**:
- `countWorkDays` كانت تستثني الجمعة والسبت فقط → العطل تظهر **غياباً**
- `doBulkMonthlyAttendance` كانت **تسجّل حضوراً بساعات كاملة في أيام العيد** — وتدخل الرواتب

المضاف: مجموعة `holidays` (مدى `from..to`)، `isHoliday`/`holidayName` بفهرس مُخزَّن (`_hdIndexDirty`)، بطاقة إدارة **داخل صفحة إعدادات الحضور**، وقاعدة أمان.

> ⚠️ **خلل أمسكه الاختبار:** أول تشغيل أعطى إجازات **مزاحة يوماً للخلف**. السبب `toISOString()` يحوّل منتصف الليل المحلي إلى UTC فيرجع اليوم السابق (الرياض +3). الحل: بناء التاريخ من **المكوّنات المحلية**. لا تستخدم `toISOString().slice(0,10)` لتواريخ محلية.

---

## 3) 🩺✈️🪜 وحدات القوى العاملة الجديدة — `public/hr-workforce.js`

ملف جديد بثلاث وحدات، على نمط `hr-suite.js`:

| الرمز | الوحدة | الصفحة |
|---|---|---|
| `[HRW1]` | 🩺 التأمين الطبي | `pg-medinsurance` · `n-medins` |
| `[HRW2]` | ✈️ الانتداب وبدل السفر | `pg-businesstrips` · `n-trips` |
| `[HRW3]` | 🪜 السلم الوظيفي والدرجات | بطاقة `gradesCard` **داخل صفحة الإدارات** (بلا عنصر شريط) |

- **التأمين الطبي**: وثيقة/فئة/مرافقون/انتهاء + لوحة تكشف **من بلا تغطية سارية** بالاسم. ملاحظة: حقل الموظف `eMedIns` هو **تكلفة شهرية** في حساب التكلفة — مكمّل لا بديل.
- **الانتداب**: طلب → اعتماد → **تصفية**، باحتساب حيّ، مربوط بالمشروع، وموصول بمحرّك الموافقات كنوع جديد **`trip`** في `APV_TYPES`.
- **السلم الوظيفي**: حقل `eGrade` في نموذج الموظف (`emp.gradeId`) + **كشف من خرج راتبه عن نطاق درجته**.

**أنماط تعلّمتها بالطريقة الصعبة (لا تكررها):**
- `ov(id)` تفتح نافذة **موجودة بالـ id** — لا تبني واحدة من عنوان ومحتوى. النمط الصحيح: بناء `div` ديناميكي وإلحاقه بـ `body` (انظر `wfModal` أو `hsEnsureSurveyModal`).
- `apvInit(type, emp)` **تُرجع** كائن الموافقة — لا تُعدّل وسيطاً ثالثاً. الاستخدام: `const ap = apvInit('trip', emp); if (ap) rec.approval = ap;`

**خلل أمسكه الاختبار:** مؤشر «موظفون مغطّون» كان يحسب غير النشطين في البسط بينما المقام «من N نشط» — كان يمكن أن يعرض «4 من 3». حُصر البسط في النشطين.

---

## 4) 🔴 مراجعة قسم المشاريع — **لم يُنفَّذ أي إصلاح**

مراجعة شاملة أُجريت بطلب المستخدم. **الحكم: 🔴 مشاكل جوهرية — ليس Production Ready.**
الدرجات: Functional 58 · Code 45 · Security 35 · Performance 30 · UX 60 · **Overall 46/100**

### Priority 1 — Critical

**١. خصم مزدوج للدفعة المقدمة** — `app.js:27084` في `calcBillingTotals`:
```js
const netAmount = totalAfterVAT - retentionAmount - advanceAmount - otherDeductions - advRecoveryAmount;
//                                                  ^^^^^^^^^^^^^ هذا خطأ
```
التلميحات في `index.html` تؤكد: `advancePct` = «يدفعها العميل **عند التوقيع**» (مقبوضة مرة واحدة)، و`advRecoveryPct` = «تُخصم من **كل مستخلص**». فخصمهما معاً خصم مزدوج.
**مُثبَت:** مستخلص 1,000,000 · احتفاظ 10% · مقدمة 20% · استرداد 20% → صافي **650,000** والصحيح **850,000** = **200,000 ريال خصم زائد لكل مستخلص**.
**الحل:** احذف `- advanceAmount` من `netAmount`. (سطر واحد — ابدأ به.)

**٢. حذف المشروع يُيتِّم 16 مجموعة** — `app.js:6824` `delPrj`:
يحذف `ledger/projects/{key}` فقط. **صفر تنظيف** لـ: `projectBOQ` · `progressBillings` · `projectPaymentSchedule` · `projectExpenses` · `projectChangeOrders` · `projectNotes` · `projectTasks` · `rfis` · `punchItems` · `qhse` · `subcontracts` · `correspondence` · `projectMonthlyCosts` · `timesheets` · `projectMaterialPurchases` · `revenueRecognition`.
كما أن `delete projEmpAssignments[k]` **يعدّل الذاكرة لا قاعدة البيانات** — يُمحى بأول `onValue`.
**الحل المقترح:** حذف ناعم (`archived:true`) + منع الحذف مع وجود مستخلصات معتمدة.

**٣. صفر قاعدة أمان لأي مجموعة مشاريع** — كلها تحت `$other`.
**مُثبَت على محاكي حقيقي (13 تأكيداً):**
- موظف عادي ومشاهد **يقرآن قيم عقود كل المشاريع + BOQ + عقود الباطن**
- **موظف HR يعدّل قيمة عقد ويحذف مشروعاً كاملاً**
- محاسب يحذف BOQ · مدير مشروع يعدّل مستخلصاً
- يمكن إنشاء BOQ ومستخلص **لمشروع غير موجود** (يتم من الأساس)

> مفارقة تستحق الانتباه: HR والمحاسبة لهما قواعد دقيقة ومختبَرة (186 اختباراً)، بينما **المشاريع — جوهر شركة مقاولات — بلا قاعدة واحدة**.

### Priority 2 — High
- **صفر حارس صلاحية** في `savePrj` (6701) و`delPrj` (6824)
- **تعارض ثلاثي في الحالات**: النموذج يحفظ 6 (`bidding·awarded·planning·active·completed·cancelled`)، `statusMap` في `renderProjects:6451` يعرف 4، والفلتر يعرض 4 مختلفة تشمل `on-hold` **الذي لا يُحفظ أبداً**. النتيجة: `bidding` يظهر نصاً خاماً ولا يمكن فلترته.
- **أداء تربيعي** — `renderProjects` يستدعي `calcProjectBillings(k)` (مسح كامل للمستخلصات) + `Object.values(projEmpAssignments).filter` لكل صف. **قياس فعلي:** 100 مشروع = 27ms · 500 = 620ms · **1000 = 3.2 ثانية** · 2000 = 13.5 ثانية (حساب صافٍ قبل DOM). **الحل:** فهرسة مسبقة `projectId → []` مرة واحدة + ترقيم صفحات.
- **لا ترقيم صفحات** · **لا تحقق من التواريخ** (نهاية قبل بداية تُحفظ) · **قيم سالبة مقبولة** · **نِسب بلا سقف** (احتفاظ 200% → صافي سالب) · **لا حراسة دورة حياة** (تعديل وحذف مشروع مكتمل مسموحان)

### Priority 3 — Medium
البحث لا يشمل الكود/العميل/رقم العقد (الاسم والوصف فقط) · لا حماية من النقر المزدوج · **مفاتيح مكررة** `clientName`/`clientAddress`/`clientVATNumber` في `savePrj` (6751-6753 كود ميت، 6774-6776 هو الفعّال) · `NaN` ممكن بمدخل غير رقمي

### ما لم يُختبر في المراجعة — `NOT TESTED`
الواجهة بتسجيل دخول حقيقي (لا بيانات اعتماد) · تحديث الصفحة والرجوع/التقدّم · تبويبان متزامنان · انقطاع الشبكة أثناء الحفظ · الاستجابة على الجوال · `project-detail.js` كاملاً (6,628 سطراً — قُرئت مقاطع مستهدفة) · المراحل (Phases) وخط الأساس · المصروفات.

---

## 4.5) 🔐 نظام الصلاحيات — المرحلة 1 (مُنجزة، **غير منشورة**) · `886b6ae`

### المشكلة التي كانت قائمة (مُثبَتة على محاكي حقيقي)
مصفوفة الصلاحيات (**119 صلاحية**) كانت **إخفاء أزرار فقط**. القواعد تفحص `role` في **147** موضعاً و`permissions` في **صفر**. الإثبات: محاسب سُحبت منه صلاحيات الحذف والترحيل من الواجهة **ظلّ يحذف القيود ويرحّل السندات**.

### الأساس: `permsMap`
قواعد RTDB **لا تفحص عضوية المصفوفات إطلاقاً** — لا سبيل لاختبار `permissions.includes(x)` داخل قاعدة. لذلك تُكتب الصلاحيات **أيضاً** كخريطة `{ key: true }`:
- `permsToMap()` / `withPermsMap()` — مساعد مركزي، طُبِّق على **مواضع الكتابة الستة** (إنشاء شركة · إنشاء مستخدم · مستخدم خدمة ذاتية · تبديل ESS · تعديل مستخدم · حفظ المصفوفة)
- **`permissions` لم تتغيّر** — تبقى مصدر الواجهة. صفر هجرة إجبارية
- `migratePermsMaps()` — ترحيل idempotent، زرّه **🔐 تفعيل الإنفاذ الخلفي** في صفحة الصلاحيات

### الإنفاذ الخلفي — 6 مجموعات
`journalEntries` · `payments` · `receipts` · `purchaseInvoices` · `projects` · `employees` — الحذف يتطلب صلاحيته (`delete_journal_entry`, `delete_payment`, …).

> **⚠️ اكتشاف معماري وجّه التصميم — احفظه:**
> في RTDB منح `.write` على **المجموعة** يُلغي تقييم القواعد الأدنى تماماً، و`.validate` **لا تُقيَّم عند الحذف**. فلا يمكن فرض صلاحية حذف من مستوى المجموعة.
> الحل المطبَّق: **جذر المجموعة → المدير فقط** (تحتاجه استعادة النسخ الاحتياطية، فهي تكتب `set(ledger/<coll>, …)` على الجذر — فُحص)، والمنطق التفصيلي على **مستوى السجل** (`$entryId` …).

**توافق خلفي مضمون:** من لا `permsMap` له تُطبَّق قواعد الدور كما كانت → **النشر لا يقفل أحداً** قبل الترحيل.

### وصل صلاحيات ميتة (34 → 31)
- `delEmp` كانت **بلا حارس إطلاقاً** → `delete_employee`
- `deleteJrnEntry` كانت **بلا حارس** → `delete_journal_entry`
- `delPrj` كانت تفحص **`edit_project`** → صُحّحت إلى `del_project` (فكان سحب «حذف مشروع» بلا أثر)

### الاختبارات
**233 ناجح · 0 فاشل** (225 قائمة + **8 جديدة** في قسم `[PERM]`): المنع الفعلي · عدم كسر العمل العادي · التوافق مع غير المُرحَّلين · الدور يبقى السقف · الاستعادة تعمل · منع الكتابة على جذر المجموعة.

### ما بقي من خطة الصلاحيات
| المرحلة | المحتوى | الحالة |
|---|---|---|
| **1** | الهجرة + الإنفاذ الخلفي + وصل الميتة | ✅ منها **31 صلاحية ميتة** باقية (`post_*`/`edit_posted_*`/`create_*` المحاسبية — تمسّ دوال الترحيل في `accounting.js`) |
| **2** | حراسة **133 صفحة** بلا حماية (nav يحرس 22 من 155) + توحيد **170 فحص دور** مباشر خلف `can()` | ⬜ |
| **3** | صفحة صلاحيات كاملة بدل الـ popup (بحث · تحديد جماعي · ملخّص · سجل تدقيق) | ⬜ |
| **4** | الاختبارات الأمنية للسيناريوهات A–E التي حدّدها المستخدم | ⬜ |

> **خطأ يستحق التذكّر:** `node --check` **مرّت** على قوسين ناقصين لأن قوساً في سطر لاحق ابتلعهما — فتغيّر المعنى بصمت. بعد أي تعديل بسكربت، **عُدّ الأقواس برمجياً** ولا تكتفِ بـ `node --check`.

---

## 5) ⏭️ الخطوات التالية (مرتّبة)

1. **⚠️ عمل غير مُودَع على هذا الجهاز:** `public/index.html` + `public/styles.css` فيهما **الوضع الغامق (مرحلة 1)** — رموز سمة، `data-theme="dark"`، زرّ `themeToggle`، حفظ في `localStorage`. **ليس من عمل جلسة الصلاحيات ولم يُودَع.** إن لم تجده على الجهاز الآخر فهو باقٍ هنا.
2. **انشر بالترتيب الإلزامي** (`886b6ae` يغيّر `database.rules.json`):
   ```bash
   git push origin main                # الاستضافة أولاً
   # تأكّد أن app.js الجديد حيّ (curl + مقارنة shasum)
   export PATH="/opt/homebrew/opt/openjdk/bin:$PATH" && npm run test:rules
   firebase deploy --only database     # القواعد ثانياً
   ```
   عكس الترتيب = قواعد تتوقّع `permsMap` وكود قديم لا يكتبها.
3. **بعد النشر — خطوة يدوية واحدة:** افتح **صلاحيات المستخدمين** واضغط **🔐 تفعيل الإنفاذ الخلفي**. قبلها الإنفاذ **غير مفعَّل لأحد** (تصميم مقصود).
4. **معاينة بشرية** — لا شيء من عمل الجلسات الأخيرة عُوين في متصفح بتسجيل دخول.
5. **أكمل خطة الصلاحيات** — المرحلة 2 (حراسة الصفحات) هي الأعلى قيمة أمنياً بعد المرحلة 1.
6. **نسخة احتياطية يدوية** قبل أول استخدام حقيقي لـ«حذف التحضير» — هدّامة، وأنت على Spark بلا احتياطي خادمي مجدوَل.
7. مؤجّلات: إتمام ثيم جسر في `app.js` · **فارق التوقيت في `today()`** (26 موضعاً؛ من يسجّل حضوره بين منتصف الليل والثالثة فجراً يُقيَّد في اليوم السابق — يمسّ الحضور والرواتب فيحتاج مهمة مستقلة).

---

## 6) 🛠️ بيئة وتشغيل — مزالق تُوفّر وقتاً

```bash
firebase serve                    # محلي (Hard refresh لرؤية التعديلات)
npm run test:rules                # 186 اختباراً — يتطلب Java
firebase deploy --only database   # يدوي دائماً!
```

- **Java للاختبارات:** `export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"` قبل `npm run test:rules`.
- **⚠️ iCloud يفسد `.git`:** المستودع داخل iCloud Drive، وقد أنشأ نسخاً مكرّرة (`refs/remotes/origin/main 2`، `.git/index 2`) عطّلت `git pull` برسالة مضلّلة *"did not send all necessary objects"*. الحل: احذف الملف ذا المسافة. إن تكرر، الحل الجذري نقل المستودع **خارج** iCloud.
- **ترتيب النشر إلزامي:** ادفع → تأكّد أن الاستضافة حيّة (`curl` + مقارنة بصمة SHA) → **ثم** انشر القواعد.
- **التخزين المؤقت للـ CDN يخدع:** بعد النشر مباشرة قد يعيد `curl` محتوى قديماً. افحص بكسر التخزين (`?cb=$(date +%s)`) وقارن بصمات `shasum`.
- **cache busting:** بعد تعديل أي ملف JS ارفع `?v=` في `index.html`.
- **تسجيل صفحة HR جديدة:** `pg-<name>` + `n-<x>` + `hrItems` + `HR_SECTION_PAGES` (لثيم جسر) + `if (pg===...)` في `nav()` + عنوان في `tt` + مرجع `R` + مستمع + `<script defer ?v=>` + **قاعدة أمان**.

---

## 7) 📌 تفضيلات المستخدم

- **ناقش قبل التنفيذ** — اعرض واحصل على تأكيد؛ لا تبنِ على أول ذكر.
- **افحص الموجود قبل الإضافة** — القسم كبير؛ وجّه للموجود بدل التكرار. *(وفّر هذا في هذه الجلسة: حذف اليوم المفرد و`ssEnhance` كانا موجودين.)*
- **الأمان أولاً** — اعرض تغييرات القواعد للمراجعة قبل النشر.
- **العربية (RTL)** في كل نصوص الواجهة · جاهزية الجوال.
