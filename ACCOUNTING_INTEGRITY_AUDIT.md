# ACCOUNTING_INTEGRITY_AUDIT.md — تدقيق سلامة المحاسبة

> **فحص فقط.** لم يُصلَح أي بند أدناه. القاعدة المتّفق عليها: لا إصلاح قبل التوثيق والاختبار والموافقة (§6).
> **التاريخ:** 2026-08-19 · **الملف المفحوص أساساً:** `public/accounting.js` (33,532 سطراً)

---

## 1. معمارية القيود الحالية

### 1.1 مسارات إنشاء القيود — سبعة

| الدالة | المصدر | السطر |
|---|---|---|
| `saveJrnEntry` / `postJrnEntry` | قيد يدوي | 6477 / 7330 |
| `createJournalForSInv` | فاتورة مبيعات | 16511 |
| `createJournalForPInv` | فاتورة مشتريات | 18673 |
| `createJournalForVoucher` | سند قبض/صرف | 19814 |
| `createJournalForCreditNote` | إشعار دائن | 16125 |
| `createJournalForDebitNote` | إشعار مدين | 16262 |
| `createJournalForPMC` | شهادة دفع مقاول باطن | 1025 **و** 2922 |

بالإضافة إلى: `postPOCRecognition` (إثبات إيراد) · `postFXRevaluation` (فروق عملة) · `postEmpExp` (مصروفات موظفين) · القيود المتكررة · الإطفاء.

**لا توجد نقطة ترحيل مركزية واحدة.** كل مسار يبني سطوره ويختار حساباته ويكتب بنفسه. أي قاعدة محاسبية جديدة (إقفال فترة، تحقّق إضافي) تحتاج تعديل **11 موضعاً** — ونسيان أحدها لا يكشفه شيء.

### 1.2 بنية القيد

```
ledger/journalEntries/$key
├── number · date · reference · description
├── lines[] : { accountCode, accountName, description, costCenter, debit, credit }
├── totalDebit · totalCredit
├── status : 'draft' | 'posted' | 'cancelled'
├── currency · exchangeRate · foreignTotal
├── sourceType · sourceKey        ← أثر المصدر
└── createdAt/By · postedAt/By
```

---

## 2. تدفّق الترحيل (Posting Flow)

نموذج `postPInv` (والنمط نفسه في `postSInv` و`postVoucher`):

```
1. فحص الصلاحية        can('post_purchase_invoice')
2. حارس الحالة          if (inv.status !== 'draft') return;
3. تأكيد المستخدم       cf2(...)
4. ⟶ كتابة ١: createJournalForPInv()  →  push(R.jrn, jrnData)
5. ⟶ كتابة ٢: update(purchaseInvoices/$key, {journalEntryKey, journalEntryNumber})
6. ⟶ كتابة ٣: update(purchaseInvoices/$key, {status:'posted', postedAt, postedBy})
7. ⟶ كتابة ٤: createInventoryMovementsForPInv()
8. toast نجاح
```

**أربع كتابات متتابعة مستقلة، بلا معاملة تجمعها.**

يوجد في الشفرة تعليق صريح يشرح أن ترتيب 4 قبل 6 **مقصود**:

> `🔒 أنشئ القيد أولاً — لا نكتب status:'posted' إلا بعد نجاحه`

هذا **نمط تعويضي يدوي** (Compensating Pattern) واعٍ ومحمود، لكنه يغطي عرَضاً واحداً فقط من عدّة.

---

## 3. التحقق من المدين/الدائن — 🔴 الثغرة الجذرية

### 3.1 ما تحرسه قاعدة البيانات

```
newData.child('totalDebit').val() === newData.child('totalCredit').val()
```

**تقارن رقمين في الترويسة فقط.**

### 3.2 ما لا تستطيع حراسته

لغة قواعد RTDB **لا تستطيع جمع مصفوفة**. فلا سبيل بنيوياً للتحقق من أن:

```
Σ lines[].debit == totalDebit    و    Σ lines[].credit == totalCredit
```

### 3.3 الحارس الحقيقي الوحيد

`accounting.js:3679` — سطر في الواجهة:
```js
if (Math.abs(dr - cr) >= 0.01) { toast('القيد غير متوازن — المدين لا يساوي الدائن', 'er'); return; }
```

### 3.4 الأثر العملي

قيد ترويسته `totalDebit = totalCredit = 1000` وسطوره تجمع `5000` **سيُقبل**. مصادر الخطر:
- خطأ برمجي في أحد مسارات الإنشاء السبعة
- استيراد أو سكربت يكتب مباشرةً
- كتابة عبر REST API لقاعدة RTDB بمفتاح صالح

**الخطورة: 🔴 حرجة** — دفتر أستاذ غير متوازن دون أي إشارة.

---

## 4. حساب الأرصدة

### 4.1 النموذج — محسوب لا مخزّن ✅

`calcCustomerBalance` (12268) و`calcVendorBalance` (17058) **يحسبان عند كل عرض** بالمرور على الفواتير والسندات في ذاكرة المتصفح. لا يوجد حقل رصيد مخزّن يُحدَّث.

**هذا قرار سليم للسلامة:** لا انحراف (drift) ممكن بين الرصيد والحركات — وهو أخطر أنواع الفساد المحاسبي.

**لكنه مكلف:** O(كل الفواتير) لكل عميل، ويستلزم تحميل كل الفواتير للذاكرة.

### 4.2 ميزان المراجعة والقوائم المالية

تُبنى بالمرور على كل القيود في الذاكرة. لا تجميع خادمي.

### 4.3 🟠 خطأ التاريخ المحلي — يمسّ حسابات مالية

```js
const today = new Date().toISOString().slice(0, 10);   // ← UTC لا محلي
```

في توقيت الرياض (UTC+3) هذا يعطي **يوم أمس** بين منتصف الليل والثالثة فجراً.

**الانتشار المقيس:**

| الملف | المواضع |
|---|---|
| `accounting.js` | **110** |
| `project-detail.js` | 46 |
| `app.js` | 26 |
| **الإجمالي** | **188 تكراراً في 182 سطراً** |

يظهر داخل حساب أعمار الديون (`calcCustomerBalance`) وتحديد الاستحقاق وحدود الفترات.

**الأثر:** فاتورة تُصنَّف «متأخّرة» قبل موعدها بيوم، أو حركة تقع في الفترة الضريبية الخطأ.

**الخطورة: 🟠 عالية** — أثر صغير الحجم، واسع الانتشار، ويصعب اكتشافه لأنه يظهر ثلاث ساعات يومياً فقط.

---

## 5. تحليل العمليات متعدّدة الخطوات

### 5.1 `postPInv` / `postSInv` — ترحيل فاتورة

| البعد | التقييم |
|---|---|
| **Atomicity Risk** | 🔴 **عالٍ** — 4 كتابات مستقلة، لا معاملة |
| **Partial Failure** | 🔴 فشل بعد الكتابة ١ ⇒ قيد يتيم. فشل بعد ٣ ⇒ فاتورة مُرحَّلة بلا حركات مخزون |
| **Duplicate Risk** | 🟠 الحارس `status !== 'draft'` يقرأ من **الذاكرة المحلية** (`window.purchaseInvoices` عبر `onValue`). نقر مزدوج سريع أو إعادة محاولة بعد مهلة ⇒ نافذة سباق تسمح بقيدين. **لا مفتاح Idempotency** |
| **Retry Risk** | 🔴 لا آلية استئناف. إعادة المحاولة تبدأ من الصفر وقد تُنشئ قيداً ثانياً |
| **Rollback** | 🟡 يدوي — `unpostSInv` موجود للمدير المالي، لكن لا تراجع تلقائي |

### 5.2 `postVoucher` — سند قبض/صرف

يضيف تعقيداً: تحديث `paidAmount` على الفواتير المُسدَّدة (تخصيص الدفعة).

| البعد | التقييم |
|---|---|
| **Atomicity** | 🔴 قيد + سند + تحديث n فاتورة = n+2 كتابة |
| **Partial Failure** | 🔴 فشل في منتصف التخصيص ⇒ سند مُرحَّل وبعض الفواتير غير مُحدَّثة ⇒ **رصيد عميل خاطئ** |
| **Duplicate** | 🔴 **الأخطر** — سداد مزدوج |

### 5.3 `postEmpExp` · `postPOCRecognition` · `postFXRevaluation`

نفس النمط: إنشاء قيد ثم تحديث حالة، بلا ذرّية.

### 5.4 ملاحظة مهمّة — القدرة موجودة وغير مستغلّة

RTDB **تدعم التحديث الذرّي متعدّد المسارات** عبر `update(ref(db), {'path/a': x, 'path/b': y})`. والنظام يستخدمها فعلاً في مواضع أخرى، ولديه `scopeUpdates()` الذي يضيف بادئة المستأجر للمفاتيح المطلقة.

**أي أن الأداة متاحة، لكن مسارات الترحيل لا تستعملها.** هذا يعني أن الإصلاح ممكن **دون تغيير مخطّط ولا انتقال لقاعدة أخرى** — وهو أهمّ استنتاج عملي في هذه الوثيقة.

---

## 6. مخاطر سلامة البيانات الأخرى

| # | الخطر | الخطورة |
|---|---|---|
| 1 | لا مفاتيح أجنبية — حذف مشروع يترك يتامى في 16 مجموعة | 🔴 |
| 2 | إقفال الفترات مُنفَّذ في JS لا في القاعدة | 🟠 |
| 3 | لا نسخ احتياطي خادمي (`dailyBackup` غير منشور على Spark) | 🔴 |
| 4 | `createJournalForPMC` مُعرَّفة مرتين — النسختان **متطابقتان حرفياً** (55 سطراً) | 🟡 تكرار غير ضارّ |
| 5 | `saveInvItem` مُعرَّفة مرتين — **مختلفتان** (55 مقابل 81 سطراً). الثانية تحجب الأولى؛ **الأولى شفرة ميتة** | 🟠 من يعدّل السطر 20758 يعدّل ما لا يعمل |
| 6 | لا ترقيم للقيود مضمون التسلسل عبر التزامن (`generateJrnNumberAtomic` موجودة — تحتاج تحقّقاً) | 🟡 |

---

## 7. الاختبارات المفقودة — 🔴 الفجوة الأخطر

| المجال | التغطية الحالية |
|---|---|
| توازن القيد | **صفر** |
| ترحيل فاتورة مبيعات/مشتريات | **صفر** |
| ترحيل سند وتخصيص الدفعة | **صفر** |
| رصيد العميل/المورد | **صفر** |
| ميزان المراجعة | **صفر** |
| دفتر الأستاذ | **صفر** |
| القوائم المالية | **صفر** |
| تكاليف المشاريع | **صفر** |
| إقفال الفترة | **صفر** |
| المستخلصات والضريبة | ✅ 27 تأكيداً (`calc.js`) |

النظام لديه **670 تأكيداً** موزّعة على قواعد الأمان ومحرر PDF وقراءة الفواتير — و**تأكيد واحد فقط لكل معادلة في `calc.js`**، بينما القلب المحاسبي بلا تغطية إطلاقاً.

**النتيجة:** لا يمكن إعادة كتابة أي منطق محاسبي اليوم بأمان، لأنه لا يوجد ما يكشف الانحدار.

---

## 8. الضمانات الموصى بها

مرتّبة بنسبة العائد إلى المخاطرة. **لا يُنفَّذ أي منها قبل Phase 5 (Golden Master) وموافقتك.**

| # | الضمانة | يعالج | المرحلة |
|---|---|---|---|
| 1 | **Golden Master** لكل عملية ترحيل | §7 | Phase 5 |
| 2 | `assertBalanced()` نقيّة تفحص **مجموع السطور** | §3 | Phase 2 |
| 3 | **نقطة ترحيل مركزية** واحدة تمرّ بها المسارات السبعة | §1.1 | Phase 4 |
| 4 | **تحديث ذرّي متعدّد المسارات** للترحيل | §5 | Phase 4 |
| 5 | **مفتاح Idempotency** (`sourceType:sourceKey`) يمنع القيد المزدوج | §5.1 | Phase 4 |
| 6 | تعطيل الزرّ + قفل تفاؤلي أثناء الترحيل | §5.1 | Phase 4 |
| 7 | `todayLocal()` بدل `toISOString().slice(0,10)` في 188 موضعاً (182 سطراً) | §4.3 | Phase 2 (بعد اختبار) |
| 8 | فحص اتّساق دوري: Σ سطور = ترويسة لكل قيد | §3 | Phase 5 |
| 9 | حذف `saveInvItem` الميتة بعد إثبات | §6.5 | Phase 2 |

**ملاحظة على البند 7:** تصحيح 188 موضعاً تغييرٌ في السلوك (تواريخ ستتغيّر). يخضع لشرط §6 كاملاً: توثيق → اختبار → إثبات المشكلة → تحديد السلوك الحالي والمستهدف → موافقة.
