# ARCHITECTURE_PROPOSAL.md — المعمارية المقترحة

> مبنيّ على `PROJECT_AUDIT.md`. لا يُنفَّذ منه شيء قبل موافقتك.

---

## 1. القرار المعماري الأول — وهو الأصعب

### المشكلة
React يتطلّب خطوة بناء (JSX + وحدات). و`CLAUDE.md` يحمل قاعدة صريحة: **لا خطوة بناء**، وقد أُزيل `build.sh` سابقاً لأن التعديلات على `public/` لم تكن تصل إلى ما يُخدَم فعلاً.

### الخيارات

| الخيار | الوصف | الحكم |
|---|---|---|
| **أ** | React بلا بناء (`htm` + CDN) | ❌ ليس React حقيقياً — بلا JSX ولا نظام وحدات ولا أدوات. يورّث كل عيوب اليوم |
| **ب** | إعادة كتابة كل شيء في React بخطوة بناء واحدة | ❌ Big Bang. 142 صفحة. انتحار مشروع |
| **ج** | **مسارا أصول متوازيان** | ✅ **الموصى به** |

### القرار: مساران متوازيان

```
public/                 ← القديم، يُخدَم كما هو (القاعدة تبقى سليمة له)
  index.html
  app.js · accounting.js · …

src/                    ← الجديد، مصدر لا يُخدَم
  domain/ · repositories/ · services/    ← تُبنى إلى ESM عادي
  ui/                                    ← React
        │
        ▼ (vite build)
public/dist/            ← ناتج البناء، يُخدَم
  domain.js             ← يستورده app.js القديم مباشرةً
  react/[module].js     ← جزر React لكل وحدة مُرحَّلة
```

**لماذا ينجح هذا:**
- القديم يبقى قابلاً للتحرير الفوري بلا بناء — القاعدة محفوظة حيث تهمّ
- الجديد يحصل على أدوات حقيقية
- `domain.js` **يستهلكه النظامان معاً** ⇒ لا ازدواج في المنطق المحاسبي (شرط §51-L)
- التراجع ممكن دائماً: احذف الجزيرة، يعود القديم

**الشرط الملزم:** `public/dist/` **يُودَع في git**. لا نكسر النشر التلقائي الحالي الذي يرفع `public/` بلا بناء.

---

## 2. طبقات النظام

```
┌─────────────────────────────────────────────────┐
│ العرض:  الصفحات القديمة  │  جزر React            │
├─────────────────────────────────────────────────┤
│ التطبيق (Use Cases) — تنسيق، صلاحيات، معاملات    │
├─────────────────────────────────────────────────┤
│ النطاق (Domain) — محاسبة نقيّة · بلا DOM بلا شبكة │
├─────────────────────────────────────────────────┤
│ عقود Repository — واجهات مجرّدة                  │
├─────────────────────────────────────────────────┤
│ التنفيذ: RTDB اليوم  →  PostgreSQL/API غداً      │
└─────────────────────────────────────────────────┘
```

القاعدة الحاكمة: **الطبقة لا تعرف ما فوقها.** النطاق لا يعرف React ولا Firebase. لا يتسرّب `DataSnapshot` ولا `DocumentReference` فوق طبقة Repository.

---

## 3. الهيكل المقترح

```
src/
├── domain/                    ← نقيّ · مختبَر · بلا React وبلا Firebase
│   ├── accounting/
│   │   ├── journalEntry.js          (توازن · ترقيم · حالات)
│   │   ├── ledger.js                (أرصدة · ميزان مراجعة)
│   │   ├── fiscalPeriod.js          (إقفال الفترات)
│   │   ├── vat.js · zakat.js · wht.js
│   │   └── __tests__/
│   ├── billing/                     ← يستوعب calc.js الحالي
│   ├── payroll/ · projects/ · inventory/ · assets/
│   └── shared/money.js              (تقريب · عملات)
│
├── application/               ← Use Cases
│   ├── accounting/postJournalEntry.js
│   ├── purchases/convertAiInvoiceToPurchase.js
│   └── shared/withPermission.js · withAudit.js
│
├── repositories/
│   ├── contracts/             ← الواجهات فقط
│   └── firebase/              ← التنفيذ الحالي
│
├── services/                  ← auth · reporting · export · storage
│
└── ui/                        ← React (يُضاف عند Phase 4)
    ├── app/ (router · providers · guards)
    ├── design-system/
    └── features/<module>/{components,hooks,pages}
```

**ملاحظة على الطلب:** الهيكل المقترح في طلبك يضع `features/` و`services/` و`repositories/` كلها تحت `src/` بمستوى واحد. أقترح تعديلاً: **`domain/` و`application/` منفصلان تماماً عن `ui/`** — لأن هذا هو ما يجعل المنطق المحاسبي قابلاً للاختبار بلا React (شرط Quality Gate في §62)، ويجعل الانتقال إلى PostgreSQL لا يمسّ الواجهة.

---

## 4. المكتبات — قرار لكل واحدة

المشروع اليوم فيه **اعتمادية تشغيل واحدة**. هذه ميزة نادرة لا تُهدَر.

| المكتبة | القرار | السبب |
|---|---|---|
| **React 18** | ✅ | الهدف المطلوب |
| **Vite** | ✅ | أداة البناء؛ أسرع ما يخدم مساراً متوازياً |
| **React Router** | ✅ | 142 صفحة بلا مسارات — التوجيه شرط للحُرّاس والصلاحيات |
| **TanStack Query** | ✅ | يستبدل 97 مستمعاً بذاكرة مؤقتة وحالات تحميل/خطأ/فراغ (§19). أكبر مكسب مفرد |
| **Zod** | ✅ | التحقق مركزياً؛ ونفس المخطّط يخدم النطاق والنماذج |
| **Zustand** | 🟡 مؤجَّل | لا نضيفه قبل إثبات الحاجة. Context يكفي للمستخدم والصلاحيات |
| **Redux Toolkit** | ❌ | تعقيد بلا مبرّر هنا |
| **مكتبة UI جاهزة** | ❌ | RTL عربي + هوية النظام قائمة في `styles.css`. نبني نظام تصميم مشتقاً منها |
| **TypeScript** | 🟡 قرار منفصل | مفيد جداً لنظام محاسبي، لكنه يضاعف كلفة أول مرحلة. **اقتراحي: JSDoc types أولاً**، وTS لاحقاً إن ثبتت الحاجة |

---

## 5. تدفّق البيانات — مثال ترحيل قيد

```
المستخدم
  ↓
React Form ──→ Zod (شكل الحقول)
  ↓
usePostJournalEntry()               ← hook
  ↓
postJournalEntry() [Application]
  ├─ withPermission('acc_post')     ← الصلاحية (وقاعدة الأمان تحرسها ثانيةً)
  ├─ fiscalPeriod.assertOpen()      [Domain]
  ├─ journalEntry.assertBalanced()  [Domain] ← مجموع السطور = الترويسة
  ↓
JournalRepository.post()            ← عقد
  ↓
FirebaseJournalRepository           ← تحديث متعدّد المسارات ذرّي
  ↓
AuditService.record()
  ↓
النتيجة → حالة الواجهة
```

**المكسب المباشر:** `assertBalanced()` يصبح دالة نقيّة مختبَرة تفحص **مجموع السطور**، لا سطراً في معالج زرّ. هذه هي ثغرة §8.2 في التدقيق، وتُغلق في طبقة النطاق قبل أي حديث عن React.

---

## 6. الجاهزية للسحابة (§51-A)

عقد Repository هو نقطة التبديل الوحيدة:

```js
// contracts/JournalRepository.js
export class JournalRepository {
  async getById(id) {}
  async listByPeriod(from, to) {}
  async post(entry) {}          // ذرّي
  async trialBalance(asOf) {}   // ← RTDB: يحسب في الذاكرة · PostgreSQL: GROUP BY
}
```

`trialBalance` مُصمَّم عمداً كطريقة على المستودع لا كحساب في الواجهة: اليوم تُنفَّذ بجلب وتجميع في الذاكرة، وغداً تصبح استعلام SQL واحداً — **بلا تغيير سطر في الواجهة**. هذا هو الاختبار العملي لـ§51-I.

---

## 7. ما لن نفعله

- ❌ Microservices · Event Sourcing · CQRS · طوابير رسائل — تعقيد بلا مشكلة تبرّره (§51-S)
- ❌ تجريد يحاول جعل RTDB وPostgreSQL متطابقتين — نعزل المنطق لا نخفي الاختلافات
- ❌ Dual Write بين نظامين — مصدر حقيقة واحد لكل وحدة (§51-M)
- ❌ تغيير مخطّط قاعدة البيانات في هذا المشروع (§39)
