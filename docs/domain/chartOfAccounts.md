# النطاق · شجرة الحسابات — توثيق الدوال

> توثيق مطلوب بشرطك التاسع. لكل دالة: السلوك الحالي · المدخلات · المخرجات ·
> الاعتماديات · الآثار الجانبية · قراءات وكتابات القاعدة · قواعد العمل · سلوك الخطأ.
>
> **المصدر:** `public/accounting.js` · **الوجهة:** `src/domain/accounting/chartOfAccounts/`
> **الإثبات:** `npm run test:char` — 44 تأكيداً تقارن بالشفرة القديمة المُشغَّلة.

---

## `ACCOUNT_TYPES` ← `COA_TYPES` (accounting.js:259)

| | |
|---|---|
| **السلوك** | ثابت: خمس مجموعات رئيسية، لكلٍّ اسم عربي وإنجليزي ولونان وبادئة رمز |
| **المدخلات** | — |
| **المخرجات** | كائن مُجمَّد |
| **الاعتماديات** | لا شيء |
| **آثار جانبية** | لا شيء |
| **قراءات القاعدة** | لا شيء |
| **كتابات القاعدة** | لا شيء |
| **قواعد العمل** | الأصول `1` · الخصوم `2` · حقوق الملكية `3` · الإيرادات `4` · المصروفات `5` |
| **سلوك الخطأ** | — |
| **التغيير عن القديم** | **لا شيء.** القيم منقولة حرفياً، ومقارَنة آلياً في الاختبار |

---

## `buildHierarchy(items)` ← `buildCoaHierarchy` (accounting.js:537)

| | |
|---|---|
| **السلوك** | يبني شجرة من قائمة مسطّحة عبر `parent` الذي يحمل **رمز** الأب لا مفتاحه |
| **المدخلات** | `Array<{code, parent?, …}>` |
| **المخرجات** | `Array<node>` — الجذور، ولكل عقدة `children` |
| **الاعتماديات** | لا شيء |
| **آثار جانبية** | لا شيء · لا يعدّل المدخلات (نسخ سطحي `{...item}`) |
| **قراءات القاعدة** | لا شيء — يعمل على مصفوفة مُمرَّرة |
| **كتابات القاعدة** | لا شيء |
| **قواعد العمل** | `parent` فارغ **أو** يشير لرمز غير موجود ⇒ **جذر** (العنصر لا يُفقَد) · الترتيب هو ترتيب المدخلات بلا فرز · الابن قد يسبق أباه في المدخلات |
| **سلوك الخطأ** | لا يرمي. رمز مكرّر ⇒ الأخير يطغى في الخريطة الداخلية (سلوك قائم، **محفوظ كما هو**) |
| **التغيير عن القديم** | **لا شيء.** الخوارزمية منقولة سطراً بسطر · 9 حالات مقارَنة بالقديم المُشغَّل |

---

## `childrenOf(accounts, code)` ← دالة داخلية في `coaBalanceRows` (accounting.js:1092)

| | |
|---|---|
| **السلوك** | أبناء رمز، مرتّبين تصاعدياً بالرمز نصّياً |
| **المدخلات** | كل الحسابات · رمز الأب |
| **المخرجات** | مصفوفة حسابات |
| **قواعد العمل** | `(a.parent \|\| '') === code` — الفراغ والـ`null` سواء · الفرز `localeCompare` على الرمز |
| **سلوك الخطأ** | لا يرمي · رمز غير موجود ⇒ مصفوفة فارغة |
| **التغيير** | **لا شيء** — كانت داخلية غير قابلة لإعادة الاستخدام، فأُخرجت كما هي |

---

## `rootsOf(accounts)` ← دالة داخلية في `coaBalanceRows` (accounting.js:1093)

| | |
|---|---|
| **السلوك** | الحسابات بلا أب، أو التي أبوها غير موجود في القائمة |
| **قواعد العمل** | اليتيم ذو الأب المفقود **يُعرَض كجذر** لا يُخفى — قرار سليم: لا يختفي حساب من ميزان المراجعة بسبب أب محذوف |
| **التغيير** | **لا شيء** |

---

## `totalsFor(accounts, account, balances)` ← دالة داخلية (accounting.js:1098)

| | |
|---|---|
| **السلوك** | مجاميع حساب. الورقة من `balances`، والتجميعي من مجموع أبنائه تنازلياً |
| **المدخلات** | كل الحسابات · الحساب · خريطة أرصدة `{code: {naturalOpening, periodDebit, periodCredit, naturalClosing, count}}` |
| **المخرجات** | `{opening, debit, credit, closing, count}` |
| **الاعتماديات** | `isHeader` فقط. **الأرصدة تُمرَّر ولا تُحسب هنا** — `calcFSBalances` تبقى في القديم حتى تُستخلَص في وحدة `ledger` |
| **آثار جانبية** | لا شيء |
| **قواعد العمل** | `nature === 'header'` ⇒ تجميعي لا يقبل حركة · الورقة بلا رصيد ⇒ يُستخدم `openingBalance` افتتاحاً وختاماً معاً · `parseFloat(…) \|\| 0` يبتلع القيم غير الرقمية إلى صفر |
| **سلوك الخطأ** | لا يرمي. **دورة في الشجرة (أب يشير لابنه) ⇒ تكرار لا نهائي** — سلوك قائم في القديم، **لم يُغيَّر**، ومُسجَّل كخطر |
| **التغيير** | **لا شيء** — 7 حسابات مقارَنة بالقديم واحداً واحداً |

---

## `validateAccount(account, ctx)` ← قواعد `saveCoaAccount` (accounting.js:850)

⚠️ **التوصيف هنا بالقراءة لا بالتشغيل** — القديمة تقرأ من DOM ولا تعمل في Node.

| | |
|---|---|
| **السلوك** | يتحقّق من حساب قبل الحفظ ويعيد أخطاءً وتحذيرات |
| **المدخلات** | `{code, type, nameAr}` · `{existing, editingKey}` |
| **المخرجات** | `{ok, errors[], warnings[]}` — **بيانات لا عرض** |
| **الاعتماديات** | `ACCOUNT_TYPES` فقط |
| **آثار جانبية** | لا شيء (القديمة تعرض `toast` وتفتح `cf2`) |
| **قراءات القاعدة** | لا شيء — القائمة تُمرَّر (القديمة تقرأ `window.chartOfAccounts`) |
| **كتابات القاعدة** | لا شيء |
| **قواعد العمل** | الرمز مطلوب ← النوع مطلوب ← الاسم العربي مطلوب ← الرمز غير مكرّر ← **بادئة الرمز تحذير لا خطأ** (القديم يسأل ويسمح بالتجاوز) · التوقّف عند أول خطأ |
| **سلوك الخطأ** | لا يرمي |
| **التغيير عن القديم** | **شكلي لا سلوكي:** القديم يعرض `toast` ويعود؛ الجديد يعيد النتيجة ليقرّر المستدعي. **لا قاعدة أُضيفت ولا حُذفت ولا غُيّر ترتيبها** |
| **حدّ صريح** | فحص التكرار من القائمة المُمرَّرة فقط — كالقديم الذي يفحص الذاكرة المحلية. **الضمان الفعلي حجزٌ ذرّي على الخادم** (`counters/coaCode`) يبقى مسؤولية المستودع لا النطاق |

---

## `canDeleteAccount(account, ctx)` ← قواعد `deleteCoaAccount` (accounting.js:913)

| | |
|---|---|
| **السلوك** | هل يجوز حذف الحساب؟ |
| **المدخلات** | الحساب · `{accounts, journalEntries}` |
| **المخرجات** | `{ok, blockers[]}` — **كل** الموانع لا أوّلها فقط |
| **قواعد العمل** | مانع ١: وجود أبناء · مانع ٢: وجود أي سطر قيد يشير لرمزه — **بأي حالة**، مسوّدة أو مُرحَّلة أو ملغاة (القديم لا يفرّق) |
| **سلوك الخطأ** | لا يرمي · حساب `null` ⇒ `{ok:false}` |
| **التغيير عن القديم** | **فرق واحد شكلي:** القديم يتوقّف عند أول مانع ويعرضه؛ الجديد يعيد الموانع كلها. **لا قاعدة تغيّرت** — الحساب الذي يُمنع في القديم يُمنع في الجديد والعكس. أُثبت باختبار «المانعان معاً يظهران معاً» |
| **🔴 ما لا تغطّيه** | تحرير حجز الرمز عند الحذف — **BUG-002**، غير مُصلَح |

---

## ما لم يُستخلَص عمداً

| الدالة | السبب |
|---|---|
| `renderChartOfAccounts` · `renderCoaTree` · `renderCoaNode` | عرض خالص — تُعاد كتابتها في Phase 7 |
| `saveCoaAccount` · `deleteCoaAccount` (الكتابة) | كتابة قاعدة بيانات ⇒ **Phase 3 (المستودعات)** |
| `coaBalanceRows` (كاملة) | تعتمد على `calcFSBalances` ⇒ تنتظر وحدة `ledger` |
| `loadDefaultAccounts` | كتابة جماعية + منطق ترحيل أسماء ⇒ Phase 3 |
| `toggleCoaNode` · `expandAllCoa` · `filterCoaTree` | حالة واجهة خالصة — لا تنتمي للنطاق |
| `exportCoaExcel` | تصدير ⇒ طبقة الخدمات |
