# MIGRATION_PLAN.md — خطة الترحيل

> لا تُنفَّذ مرحلة قبل موافقتك. كل مرحلة تنتهي بتقرير: ما اكتمل · الملفات · الاختبارات · المخاطر · فحص الانحدار.

---

## تقييم استراتيجية الترحيل (§27 — «لا تستخدم Strangler بشكل أعمى»)

فحصتُ الشروط الستة قبل اختيار الاستراتيجية:

| السؤال | الجواب المقيس |
|---|---|
| هل يمكن تشغيل القديم وReact معاً؟ | ✅ نعم — كل صفحة `<div class="pg" id="pg-*">` مستقلة، وReact يُركَّب داخل واحدة منها |
| هل يسمح التوجيه بالانتقال التدريجي؟ | 🟡 جزئياً — `nav()` يبدّل أصنافاً؛ يمكن اعتراضه ليمرّر صفحات مُرحَّلة إلى React |
| هل توجد حالة مشتركة؟ | 🔴 **نعم بشدّة** — 2,356 متغيّراً عاماً |
| هل توجد مكوّنات مشتركة؟ | 🔴 نعم — `toast` · `ov` · `cf2` · `ssEnhance` · `esc` |
| هل المنطق مرتبط بالواجهة؟ | 🔴 نعم — 2,522 معالجاً مضمّناً |
| هل توجد أجزاء قابلة للعزل؟ | ✅ نعم — البيانات الرئيسية (موردون · عملاء · شجرة حسابات) |

**الحكم:** Strangler **ممكن تقنياً لكنه خطر إن بدأ بالواجهة.** الحالة المشتركة (2,356 متغيّراً) تجعل أي جزيرة React مبكرة تعتمد على جسر هشّ.

**الاستراتيجية المختارة — «النطاق أولاً، ثم الجزر»:**

> نستخلص طبقات النطاق والمستودع **تحت** النظام القائم أولاً (بلا لمس الواجهة)، ثم نبني جزر React فوق طبقات مستقرّة ومختبَرة.

**لماذا:** يقلب ترتيب المخاطرة. الجزيرة الأولى ستستهلك نطاقاً مختبَراً لا شفرةً مجهولة، والقديم يستفيد من نفس التحسين فوراً. وإن توقّف مشروع React غداً، تبقى المراحل 1–3 مكسباً صافياً — **وهي أيضاً الشرط المشترك لمشروع PostgreSQL/الاستضافة السعودية**.

---

## Phase 0 — التدقيق ✅ (هذه المرحلة)
**المخرجات:** `PROJECT_AUDIT.md` · `ARCHITECTURE_PROPOSAL.md` · `MIGRATION_PLAN.md` · `MIGRATION_STATUS.md`
**تغييرات في الشفرة:** لا شيء.

---

## Phase 1 — خط الأساس والسلامة
**الهدف:** أن يصبح التراجع ممكناً دائماً، وأن يوجد مرجع رقمي للمقارنة.

| | |
|---|---|
| **الحالة الآن** | 10 إيداعات غير مدفوعة · لا وسم أساس · لا فصل بيئات · لا نسخ احتياطي خادمي |
| **الهدف** | كل شيء مدفوع · وسم `migration/baseline` · لقطة أرقام مالية مرجعية |
| **يُنشأ** | `docs/BASELINE.md` · `tests/fixtures/` (بيانات مُصنَّعة لا إنتاجية) |
| **لا يتغيّر** | أي شفرة تشغيل |
| **المخاطر** | منخفضة |
| **القبول** | الوسم موجود · الاختبارات السبع تعمل من مجلد نظيف · أرقام الأساس موثّقة |

**لقطة الأساس المالية:** ميزان مراجعة · مجموع مدين/دائن · أرصدة العملاء والموردين · إجماليات الفواتير · تكاليف المشاريع — تُحفَظ كأرقام مرجعية تُقارَن بعد كل مرحلة.

**التقدير:** 1–2 يوم.

---

## Phase 2 — استخلاص النطاق المحاسبي 🎯
**الهدف:** إخراج قواعد المحاسبة إلى وحدات نقيّة مختبَرة — بلا لمس الواجهة.

| | |
|---|---|
| **الحالة** | المنطق مدفون في `accounting.js` (33,532 سطر) مختلطاً بالـDOM |
| **الهدف** | `src/domain/accounting/*` نقيّة · `public/dist/domain.js` يستوردها القديم |
| **يُنشأ** | وحدات النطاق + اختبارات Golden Master |
| **يُعدَّل** | `accounting.js` يستدعي النطاق بدل نسخته المحلّية — **بلا تغيير سلوك** |
| **لا يُحذف** | أي دالة؛ المرشّحات في `DEAD_CODE_CANDIDATES.md` |
| **المخاطر** | 🟠 متوسطة — يمسّ المحاسبة. يُخفَّف بـGolden Master **قبل** أي نقل |

**الترتيب الإلزامي لكل دالة:** اقرأ → وثّق → اختبر السلوك الحالي (Golden Master) → انقل → قارن → أودِع.

**المكسب المستقل:** تُغلق ثغرة §8.2 — `assertBalanced()` يفحص مجموع السطور، ويصبح مختبَراً.

**القبول:** كل تأكيدات الأساس المالية متطابقة · لا تغيير في الواجهة · `test:calc` و`test:domain` خضراوان.

**التقدير:** 3–4 أسابيع. **هذه أهم مرحلة في المشروع كلّه.**

---

## Phase 3 — طبقة Repository
**الهدف:** عزل RTDB خلف عقود.

| | |
|---|---|
| **الحالة** | 336 استدعاء `ref()` و97 `onValue` مبعثرة |
| **الهدف** | `contracts/` + `firebase/` — الوحدات المُرحَّلة تمرّ عبرها |
| **حرج** | غلافا `ref`/`scopeUpdates` (عزل المستأجرين) يُنقَلان كما هما بلا تعديل |
| **المخاطر** | 🟠 متوسطة |
| **القبول** | `test:rules` (186) أخضر · لا تسرّب `DataSnapshot` فوق الطبقة |

**التقدير:** 2–3 أسابيع.

---

## Phase 4 — أساس React (بلا وحدات بعد)
**الهدف:** الهيكل فقط: Vite · Router · Providers · ErrorBoundary · Auth · Layout · نظام تصميم أوّلي.

**قرار حاسم:** `public/dist/` يُودَع في git كي لا ينكسر النشر التلقائي.
**القبول:** صفحة React واحدة فارغة تعمل داخل التطبيق القديم · القديم سليم تماماً.
**التقدير:** 1–2 أسبوع.

---

## Phase 5 — أول وحدة: شجرة الحسابات
**لماذا هذه تحديداً** — وفق `المخاطرة × التعقيد × الاعتمادية × الأهمية`:

| المعيار | التقييم |
|---|---|
| المخاطرة المالية | 🟢 **منخفضة** — بيانات رئيسية تُعرِّف البنية ولا تحسب مالاً |
| التعقيد | 🟡 متوسط — شجرة · نموذج · تحقق · بحث |
| الاعتمادية | 🔴 **عالية** — كل شيء يقرأ منها |
| الأهمية | 🟠 عالية |

الاعتمادية العالية **ميزة هنا لا عيباً**: إثبات عقد المستودع على المجموعة التي يقرأها الجميع يعني أن كل وحدة لاحقة ترث عقداً مُثبتاً. وهي تمارس الطبقات كلها (مستودع · تحقق · صلاحيات · جدول · نموذج · حالات تحميل/فراغ/خطأ) **دون لمس أي معادلة مالية**.

**البدائل المرفوضة:** الإعدادات (بسيطة جداً — لا تثبت شيئاً) · لوحة التحكم (تقرأ من كل شيء — تكشف الجسر الهشّ مبكراً) · قيود اليومية (أخطر وحدة — تُترك للأخير).

**القبول:** الوظائف متطابقة مع القديم · لا تغيير في البيانات · الصلاحيات تعمل · علم ميزة للتراجع الفوري.
**التقدير:** 2–3 أسابيع (تشمل تمهيد نظام التصميم).

---

## Phase 6 فما بعد — وحدة بعد وحدة

الترتيب المقترح (**بعد** مراجعة نتائج Phase 5):

```
6.  الموردون والعملاء        (بيانات رئيسية · منخفض الخطورة)
7.  المخزون والأصناف
8.  فواتير المشتريات          ← أول وحدة تُنشئ قيوداً
9.  فواتير المبيعات
10. سندات القبض والصرف
11. قيود اليومية              ← الأخطر · بعد نضج كل الطبقات
12. دفتر الأستاذ وميزان المراجعة
13. القوائم المالية والتقارير
14. المشاريع والمستخلصات
15. الموارد البشرية والرواتب   ← الأكبر حجماً
16. لوحة التحكم               ← الأخيرة (تعتمد على الكل)
17. الإعدادات
```

كل وحدة تمرّ بنفس الدورة: تحليل → خطة → تنفيذ → اختبار → مقارنة → إصلاح → توثيق → موافقة → التالية.

---

## تقدير الجهد الكلي — بصراحة

| المرحلة | التقدير |
|---|---|
| 1 — الأساس | 1–2 يوم |
| 2 — النطاق | 3–4 أسابيع |
| 3 — المستودعات | 2–3 أسابيع |
| 4 — أساس React | 1–2 أسبوع |
| 5 — أول وحدة | 2–3 أسابيع |
| 6–17 — باقي الوحدات | **6–12 شهراً** |

**الإجمالي الواقعي: 8–14 شهراً لمطوّر واحد متفرّغ.**

هذا ليس تشاؤماً بل حساب: 142 صفحة، و2,522 معالجاً مضمّناً، و65,000 سطر في ملفين. من يقدّرها بأسابيع يفترض «تحويل صيغة»، وهو ما ليست عليه.

**اقتراحي الصريح:** نفّذ المراحل 1–3 (6–8 أسابيع) ثم **أعد تقييم القرار**. عندها سيكون بين يديك نطاق محاسبي نظيف ومختبَر ومستودعات معزولة — وهي مكسب صافٍ سواء أكملت إلى React أو حوّلت الأولوية إلى PostgreSQL والاستضافة السعودية.

---

## ⚠️ تحذير: مشروعان متنافسان

يوجد مساران مطروحان الآن:
- **أ** — الترحيل إلى React (هذه الوثيقة)
- **ب** — PostgreSQL + استضافة داخل السعودية (`دراسة استضافة النظام داخل السعودية.docx`)

**تنفيذهما معاً في آن واحد هو أسرع طريق لفشل الاثنين.** لكن **المراحل 1–3 شرط مشترك لهما**، ولا يضيع منها شيء أياً كان قرارك.

القرار المطلوب منك **بعد Phase 3 لا قبلها**: أيّهما يستكمل أولاً.
